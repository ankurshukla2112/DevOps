Q.1) Can we use ALB as ingress in Kubernetes?
-> yes, you can use AWS Application Load Balancer (ALB) as an Ingress in Kubernetes - using the AWS Load Balancer Controller. 

How it works: the AWS Load Balancer Controller is a Kubernetes controller that automatically provisions ALBs in response to kubernetes ingress or service resources.

It supports:
- path-based routing
- Host-based routing
- SSL termination
- Redirects
- WebSockets 

Steps to use ALB as Ingress:

1) Install the AWS Load Balancer Controller: 
- Add the IAM policy for the controller to your worker nodes or create an IAM ROLE WITH IRSA (IAM Roles for Service Accounts).
- Deploy the controller using Helm.
2) Annotate your Ingress resources:
- use annotations to configure ALB-specific behaviours, like target group settings, SSL, etc.
3) use alb.ingress.kubernetes.io/* annotations:

---------------------------------------------------------------------------------------------

Q.2) What is the purpose of creating a user in a Dockerfile?
-> Creating a user in a Dockerfile serves both security and best-practice purposes.

1) Security: Avoid Running as Root
By default, Docker container run as the root user unless you explicitly specify another user. Running as root inside a container can be risky:
- if the container is compromised, an attacker gets root access inside the container.
- with certain Docker misconfigurations, it could lead to escalations on the host.
Creating and using a non-root user reduces the attack surface.

2) FIlesystem Permissions
Creating a specific user helps ensure that:
- App processes only have access to the files they need.
- volumes and mounted directories have correct ownership.

3) Compliance & Auditing
Many orgs (esp. in regulated industries) require containers not to run as root - for PCI, HIPAA, etc.

Example Docker file ->

FROM node:18

# create a user and group
RUN grouped -r appgroup && user add -r -g appgroup appuser

# set worker and give ownership
WORKDIR /app
COPY . .
RUN chown -R appuser:appgroup /app

# Switch to the new user
USER appuser

CMD ["node", "server.js"] 

-------------------------------------------

Q.3) if we have a file with a name other than Dockerfile, can you run it to create a Docker image? If yes, please mention the command.?
-> yes, we can absolutely build a Docker image from a file with a name other than Dockerfile.
We just need to specify the file name using the -f option in the docker build command.
Example - suppose your docker file is named MyDockerfile instead of the default Dockerfile:
-> docker build -f MyDockerfile -t my image:latest .
-> docker build -f <custom_filename> -t <image_name> <build_context>

--------------------------------------------------------------

Q.4) in Docker run, what does -P signify?
-> in the docker run command, the -p flag tells Docker to publish all exposed ports of the container to random ports on the host machine. When we build a Docker image, the Dockerfile may contain one or more EXPOSE instructions, like: EXPOSE 80, EXPOSE 443
This declares that the containers listens on ports 80 and 443 internally.
Docker run -p my image

--------------------------------------------------------------

Q.5) in Terraform, what do explicit and implicit mean?
-> in Terraform, the terms explicit and implicit refer to how dependencies between resources are defined, how terraform knows the order in which to create, modify, or destroy resources.
-> An implicit dependency is created automatically by terraform when one resource references another in its configuration.

Example -> 
Resource "aws_vpc" "my_vpc" {
  cidr_block = "10.0.0.0/16"
}

Resource "aws_subnet" "my_subnet" {
  vpc_id = aws_vpc.my_vpc.id
  cidr_block = "10.0.1.0/24"
}

- aws_subnet.my_subnet depends on aws_vpc.my_vpc
- Because it references aws_vpc.my_vpc.id
- Terraform automatically knows to create the VPC first, then the subnet
This is implicit dependency - no extra keyword needed.

-> Explicit Dependency ->
An explicit dependency is when you manually tell Terraform that one resource depends on another, using the depends_on argument.

Resource "aws_instance" "web" {
  Ami = "ami-123456"
  Instance_type = "t2.micro"
  depends_on = [aws_s3_bucket.logs]
}

Resource "aws_s3_bucket" "logs" {
  Bucket = "my-log-bucket"
}

- Even though the EC2 instance doesn't reference the S3 bucket.
- The depends_on argument explicitly tells Terraform to create the bucket first.
- This is explicit dependency - you manually define the order.

--------------------------------------------------------------

Q.6) What is a Pod in Kubernetes? Create a pod.yaml for a single-container pod running Nginx.
-> A Pod is the smallest deployable unit in Kubernetes, it represents one or more containers that share: the same network namespace (IP address and ports), Storage volumes, And can communicate via localhost.
Typically,  a pod contains one container, but it can also contain multiple tightly coupled containers (like a sidecar pattern)
A Pod = one or more containers + shared resources (network, storage, metadata)

Example: pod.yaml for a single-container Nginx Pod

apiVersion: v1
Kind: Pod
Metadata:
   Name: nginx-pod
   Labels:
     App: nginx
Spec:
  Containers:
    -name : nginx-container
     Image: nginx:latest
     Ports:
       - containerPort: 80

Explanation:
- apiVersion:v1 -> Pods belong to the core API group.
- kind.Pod -> Defines the resource type.
- metadata -> contains identifying data like name and labels.
- spec -> Defines the desired state (containers, images, ports, etc.)
- containers -> List of container definitions (here just one: Nginx).
- containerPort: 80 -> Exposes port 80 inside the Pod for HTTP traffic.

-> to create the pod
Kubectl apply -f pod.yaml

-> To verify:
Kubectl get pods
Kubetcl describe pod nginx-pod
Kubectl logs nginx-pod

--------------------------------------------------------------  

Q.7) What is a Deployment in Kubernetes? Write a deployment.yaml for deploying 3 replicas of an Nginx container.
-> A Deployment in Kubernetes is a higher-level abstraction that manages a set of identical pods. It ensures that:
- The desired number of Pods are always running (self-healing).
- you can update (rolling updates) or rollback your applications safely.
- it provides scaling (replicas) and version control for pods.
- Deployment is stateless set

-> A deployment manages ReplicaSets, and each ReplicaSet manages Pods.

apiVersion: apps/v1
Kind: Deployment
Metadata:
   Name: nginx-deployment
   Labels:
      App: nginx
Spec:
  Replicas: 3
  Selector:
     matchLabels:
       App: nginx
  Template:
    Metadata:
      Labels:
        App: nginx
    Spec:
      Containers:
        - name: nginx
          Image: nginx:latest
          Ports:
            - containerPort: 80

To Deploy ->
Kubectl apply -f deployment.yaml

To verify ->
- kubectl get deployments
- kubectl get pods
- kubectl describe deployment nginx-deployment

-----------------------------------------------------------------

Q.8) What is a Service in Kubernetes, and what are the types of Services?
-> A service in Kubernetes is an abstraction that defines a stable network endpoint to access one or more Pods. Pods are ephemeral - they can be created, destroyed, or rescheduled at any time, so their IP address change frequently.
- Cluster ip -> Exposes the service internally within the cluster, only inside the Cluster.
- NodePort -> Exposes the service on a static port (30000-32767) on each Node's IP, From outside the cluster using <NodeIP>:<NodePort>
- LoadBalancer -> Provisions an external load balancer (from cloud provider), From outsode the cluster via public IP.

- Example - ClusterIP Service for Nginx Deployment
 
apiVersion: v1
Kind: Service
Metadata:
  Name: nginx-service
Spec:
  Selector:
     App: nginx
  Ports:
    - protocol: TCP
      Port: 80
      targetPort: 80
  Type: ClusterIP

- To Create ->
Kubectl apply -f service.yaml

- To Verify:
Kubectl get services
Kubectl describe service nginx-service

--------------------------------------------------------

Q.9) When would you use each type of Kubernetes Service (ClusterIP, NodePort, LoadBalancer, ExternalName)?
->
- ClusterIP -> use it when you want internal-only-communication within the cluster, Microservices talking to each other inside the cluster (e.g., frontend -> backend -> database), internal APIs or system components that don't need public access.

- NodePort -> use it when you want to access a service from outside the cluster, but don't have a cloud Load balancer, best for Development or testing environments, Bare-metal or on-perm clusters (no cloud LB integration), Temporary external access for debugging.

- LoadBalancer -> use it when you need public internet access with a cloud-managed load balancer, best for production workloads in cloud environments (AWS, GCP, Azure), Exposing web apps, APIs, or ingress controller publicly.

----------------------------------------------------------
 
Q.10) Write a simple Terraform script to provision a virtual machine on AWS.
-> main.tf

# -------------------------
# 1. Provider Configuration
# -------------------------

Provider "aws" {
  Region = "us-east-1"  # Change to your desired AWS region
}

# --------------------------
# 2. EC2 Key Pair (optional)
# --------------------------
# you can use an existing key pair or create one
Resource "aws_key_pair" "my_key" {
  key_name = "my-key"
  public_key = file("~/.ssh/id_rsa.pub") # path to your public SSH key
}

# --------------------------------
# 3. Security Group
# --------------------------------
Resource "aws_security_group" "vm_sg" {
    Name = "vm_sg"
    Description = "Allow SSH and HTTP access"
}
# ----------------------------------
# 4. EC2 Instance
# ----------------------------------
Resource "aws_instance" "my_vm" {
  Ami = "ami-87yhuh78yyuh"  # Amazon Linux 2 in us-east-1
  instance_type = "t2.micro"  # Free-tier eligible
  key_name = aws_key_pair.my_key.key_name
  security_groups = [aws_security_group.vm_sg.name]

  Tags = {
    Name = "MyTerraformVM"
  }
}

- Steps to Run:
- Initialise Terraform -> terraform init
- Preview the changes -> terraform plan
- Apply the configuration -> terraform apply

- A security group allowing SSH (22) and HTTP (80)

-----------------------------------------------------------------

Q.11) Explain port, targetPort, and nodePort in a Kubernetes service.
->
- Port -> 
- Definition: The port that the Service exposes inside the cluster.
- Purpose: Clients inside the cluster use this port to access the service.
- Applies to: All service types (ClusterIP, Nodeport, Loadbalancer)
- Example:
    Ports:
      - port: 80

- targetPort ->
- Definition: The port on the pod that the service forwards traffic to.
- Purpose: Maps Service traffic to the correct container port inside the pod.
- Type: can be a number (port number) or a name (from the container port)
- Example:
  Ports:
    - port: 80
      targetPort: 8080

- nodePort (only for NodePort or LoadBalancer)
- Definition: A statistical port on each node that maps to the service.
- purpose: Allows external traffic to reach the service via <NodeIO>:<nodePort>.
- Range: Default is 30000-32767
- Example: 
  Ports:
    - port: 80
      targetPort: 8080
      nodePort: 32080

--------------------------------------------------------------

Q.12) How would you expose a Kubernetes application externally?
-> Exposing a Kubernetes application externally depends on how you want it to be accessible - either within the cluster, from outside via a node IP, or through a cloud load balancer. Here's a clear breakdown with options and examples:
-> Using a NodePort Service ->
 - Expose the application on a static port on every node.
 - Can be accessed externally via <NodeIP>:<NodePort>.
 - Simple and works for testing or on-perm clusters.

- apiVersion: v1
  Kind: Service
  Metadata:
    Name: nginx-nodeport
  Spec:
    Type: NodePort
    Selector:
      App: nginx
    Ports:
      - port: 80   # Service port
        targetPort: 80   # Pod container port
        nodePort: 30080  # External node port

-> Using a LoadBalancer Service (cloud environments)
 - Creates a cloud provider load balancer automatically.
 - Provides a public IP to access the application externally.
 - Ideal for production apps in AWS, GCP, Azure, etc.

- apiVersion: v1
  Kind: Service
  Metadata:
     Name: nginx-lb
  Spec:
    Type: LoadBalancer
    Selector:
       App: nginx
    Ports:
      - port: 80
        targetPort: 80

Note -> After creation, "kubectl get service nginx-lb" will show a public IP.

-> Using an Ingress
- Provides HTTP/HTTPS routing for multiple services using hostnames or paths.
- Requires an Ingress Controller (like NGINX Ingress Controller)
- More flexible than NodePort or LoadBalancer.

- apiVersion: networking.k8s.io/v1
  Kind: Ingress
  Metadata:
     Name: nginx-ingress
  Spec:
    Rules:
      - host: myapp.example.com
        http:
          Paths:
            - path: /
              pathType: Prefix
              Backend:
                 Service:
                    Name: nginx-service
                    Port:
                       Number: 80

- note -> Access it via: http://myapp.example.com

-----------------------------------------------------------  

Q.13) What is Helm, and what are its components (Chart, Repository, Release)?
-> Helm is a package manager for Kubernetes, similar to how:
Apt works for Ubuntu, yum works for RHEL, 6pm works for Node.js
Helm helps you define, install, and manage Kubernetes applications more easily and consistently, Instead of manually writing multiple YAML files (for Deployments, Services, Ingress, etc.), you can use Helm to bundle them together into a single reusable package called a Chart.
- package complex Kubernetes manifests into a single deployable unit (a chart)
- Manage configuration using values files.
- Perform upgrade, rollbacks, and version control.
- Simplify deployments (e.g., "helm install nginx" instead of 5 YAMLs)

-> Helm Components ->
- Chart -> A package of pre-configured Kubernetes resources. It contains templates (YAML files), metadata, and default configuration values.
- Repository -> A Collection of Charts stored at a central or private location. Helm can pull Charts from these repos.
- Release -> A running instance of a Chart in a Kubernetes cluster. Every install of a Chart creates a new Release.

-------------------------------------------------------

Q.14) What is the difference between EXPOSE in a Dockerfile and docker run -p?
-> EXPOSE (in Dockerfile)
Expose is a metadata instruction in a Dockerfile that documents which port(s) the container intends to listen on at runtime.
It does NOT actually publish the port - it just tells users (and tools like Docker Compose) that "this container listen on this port"
- Example ->
 FROM nginx:latest
 EXPOSE 80

- This means: "My application inside the container listen on port 80."
- But it's only informational - it doesn't open or map any ports to the host machine.

- Note -> you can check it with: docker inspect <image-name>
And look under "ExposedPorts"

-> docker run -p ->
The -p (or --publish) flag actually Maps a port from your host machine to the container port, enabling external access.
Syntax: docker run -p <host_port>:<container_port> <image>
Example: docker run -p 8080:80 nginx

- Maps port 8080 on your host -> port 80 inside the container.
- You can now access Nginx at http://localhost:8080.

- In short:
- EXPOSE -> "My app listens on this port inside the container"
- -p -> "Make this container's port reachable from outside" 

-------------------------------------------------------------

Q.15) How do you run Nginx on a Linux server using Docker?
-> Make sure: Docker is installed and running on your Linux server
Docker --version
Systemctl status docker
- you have internet access to pull images from Docker Hub.
- Download the latest official Nginx image from Docker Hub: (docker pull nginx:latest)
- Run an Nginx container and expose it to the host machine: (docker run -d -p 8080:80 --name mynginx nginx)

Explanation:
- --name mynginx: Names your container "mynginx."
- p 80:80: maps port 80 on the host -> port 80 inside the container
-d: Runs the container in detached mode (in background)
- nginx: the image name.

After that, open your browser and go to:
http://<your_server_ip>

- Manage the container
- docker ps
- docker stop mynginx
- Docker start mynginx
- Docker logs mynginx
Docker exec -it mynginx bash

---------------------------------------------------------------

Q.16) Explain HTTP, HTTPS, TCP, and UDP with examples.
-> 
1. HTTP (Hyper Text Transfer Protocol)
Http is an application-layer protocol used for transmitting web pages and data over the internet.
- key points:
- Runs on port 80 by default.
- Stateless: Each request is independent.
- plain text transmission - not secure
- Uses Tcp underneath for reliable delivery.

When you open this in a browser:
1. Your browser sends an HTTP request to the web server.
2. The server responds with an HTML page.

2. HTTPS(Hyper Text Transfer Protocol Secure)
-> HTTPS is the secure version of HTTP, which encrypts data using SSL/TLS (Secure Socket Layer / Transport Layer Security)
- Key points:
- Runs on port 443.
- Encrypts all communication between browser and server.
- Protects against data theft and man-in-the-middle attacks.
- still uses TCP underneath for reliability.
- A TLS handshake happens (encryption setup)
- Then normal HTTP communication occurs - but encrypted

3. TCP (Transmission Control Protocol)
TCP is a transport-layer protocol that provides reliable, ordered, and error-checked delivery of data between applications.

- Key points:
- Connection-Oriented -> Requires a handshake (SYN, SYN-ACK, ACK)
- Guarantees data arrives in order and without loss.
- Slower than UDP because of reliability overhead.

Example:
When you open a website (https://example.com):
- your browser uses TCP to connect to port 443.
- TCP ensures all packets arrive correctly.
- If packets are lost, TCP retransmits them.
- Used by:
- HTTP/HTTPS
- SSH
- FTP
- Email (SMTP, IMAP, POP3)

4. UDP (User Datagram Protocol)
UDP is a connectionless transport protocol - faster but unreliable compared to TCP.
- Key points:
- No handshake or delivery guarantee.
- Data can be lost or arrive out of order.
- Runs on various ports depending on the service

Example ->
- DNS -> Uses UDP port 53 (fast name lookups)
- Video streaming, VoIP, and online gaming -> Use UDP for low latency.

- Used by ->
- DNS
- Streaming (Youtube, Zoom, etc)
- Gaming
- DHCP

- Analogy ->
Like sending a postcard - no guarantee it reaches, but it's fast.

-------------------------------------------------------------------------
  
Q.17) What is a Dockerfile? Write a basic Dockerfile for a Node.js application.
-> A Dockerfile is a text file that contains a set of instructions for building a Docker image.
Think of it like a recipe - it tells Docker:
- What base image to use
- which dependencies to install
- How to copy source code
- How to run your application
- When you run docker build, Docker reads the Dockerfile line by line to create an image.

Docker file ->

# use the official Node.js LTS image as base
FROM node:18

# Set working directory inside container
WORKDIR /usr/src/app

# Copy package.json and package-lock.json
COPY package*.json ./

# Install dependencies
RUN nam install --production

# COPY all app files into container
COPY . .

Expose the app port
EXPOSE 3000

# Define the command to run the app
CMD ["node","server.js"]

-- How to Build and Run
- Build the Docker image:
- docker build -t my-node-app .

- Run the container:
- docker run -d -p 3000:3000 my-node-app

Access the app:
Open your browser and go to:
http://localhost:3000

--------------------------------------------------------

Q.18) What is a base image in Docker? Which base image would you use for Python or Node.js?
-> A base image is the starting point for building a Docker image.
It's like the foundation of a house - everything else (tour app code, dependencies, configurations) is built on top of it.

-- Two types of Base images
1) Official Base images:-
Maintained by Docker or verified publisher (e.g., ubuntu, alpine, node, python, etc.)
-> Reliable and secure.

2) Customer Base Images:
Created by you or your organisation for internal use (e.g., company-standard images with pre-installed tools).

-- Why Base Image matters
Choosing the right base image affects:
- Image size
- Security (official images are more trusted)
- Performance (lightweight images start faster)
- Compatability with your app's language/runtime

-- For python Applications
1) Full image (Debian-based):
- FROM python:3.10

2) Slim version (smaller)
- FROM python:3.10-slim

3) Alpine version (minimal):
- FROMpython3.10-alpine

-- Fir Node.js Applications
1) Full image (Debian-based):
- FROM node:18

2) Slim version:
- FROM node:18-slim

3) Alpine version:
- FROM node:18-alpine

----------------------------------------------------------

Q.19) How do you check for open ports on a Linux system?
-> using command "netstat -tlnup"
- t -> TCP port
- u -> UDP port
- l -> Listening Ports only
- n -> show numeric address (no DNS lookups)

-------------------------------------------------------------

Q.20) What are the benefits of using a firewall?
-> A firewall is a network security system (hardware or software) that monitors and controls incoming and outgoing network traffic based on predefined security rules.
Think of it as a security guard at je gate - it decides which traffic can or leave your system or network.

- Benefits of using a Firewall ->
- Protects Agains Unauthorised Access
- Monitors Network Traffic
- Blocks Malicious and Attacks
- Defines Access Control Policies
- Prevents Data Exfiltration
- Segregate Network Zones
- Helps with Compliance and Auditing.

----------------------------------------------------------------

Q.21) What is the use of Ingress and Ingress Controller in Kubernetes?
-> Ingress in kubernetes ->
Ingress is a Kubernetes API object that manage external access to service within your cluster - usually HTTP and HTTPS traffic.
- simply analogy
- service: Like a phone extension (each app has its own number).
- ingress: Like the receptionist who takes calls (HTTP requests) and forwards them to the right extension based on the caller's input (URL or hostname).
- What Ingress Does ->
- Handles URL-based routing
- Supports multiple domains or subdomains
- Enables TLS/SSL termination (HTTPS)
- Works as a reverse proxy
- Reduces need for multiple LoadBalancers or NodePorts

- Ingress YAML Example

apiVersion: networking.k8s.io/v1
Kind: Ingress
Metadata:
   Name: app-ingress
   Annotations:
      Nginx.ingress.kubernetes.io/rewrite-target: /
Spec:
  Rules:
  - host: my apps.example.com
    http:
      Paths:
      - path: /app1
        pathType: Prefix
        Backend:
          Service:
            Name: app1-service
            Port:
              Number: 80
      - path: /app2
        pathType: Prefix
        Backend:
          Service:
            Name: app2-service
            Port:
              Number: 80

- How it works:
- Request to my apps.example.com/app1 -> goes to app1-service
- Request to my apps.example.com/app2 -> goes to app2-service

-> What is an Ingress Controller->
- The Ingress resource itself is just a set of rules - it does nothing by itself, you need an Ingress Controller to implement those rules and actually handle the routing.
Ingress Controller = the engine that makes Ingress work
It watches for Ingress resources and configures an underlying proxy/load balancer (like Nginx or Traefik) to route traffic.

- Quick Example flow->

User (Browser) -> [ Load Balancer / External IP ] -> [ Ingress Controller (Nginx) ] -> [ Ingress Rules ] -> [ Kubernetes Service (app1-service) ] -> [ Pod (app1) ]

- In short
- Ingress = Defines how traffic should be routed.
- Ingress Controller = Actually routes the traffic based on those rules.

----------------------------------------------------------------
 
Q.22) Explain the Kubernetes controllers: Deployment, StatefulSet, ReplicaSet, and DaemonSet.
-> In Kubernetes, controllers are control loops that continuously monitor the desired state (defined in YAML manifests) and the actual cluster state, and make changes to bring the actual state closer to the desired one.
- The main workload controllers you'll deal with are:
Deployment, Statefulset, Replicaset, and daemonSet.

1) ReplicaSet ->
Purpose: Ensures a specified number of identical pods are running at all times, it's the basic building block for maintaining replicas (copies) of a pod, A ReplicaSet automatically creates or deletes pods to maintain the desired count, If one pod dies, the replicaSet replaces it.

Apiversion: apps/v1
Kind: Replicaset
Metadata:
  Name: nginx-replicaset
Spec:
   Replicas: 3
   Selector:
      matchLabels:
        App: nginx
   Template:
     Metadata:
       Labels:
         app: nginx
     Spec:
       Containers:
        - name: nginx
          Image: nginx:latest

Note -> We rarely create Replicases directly - instead, Deployments manage ReplicaSets automatically (to handle updates)

2) Deployment ->
- Purpose - Manages ReplicaSets and provides rolling updates and rollbacks for stateless applications. Most common controller for stateless workloads. When you change a Deployment (e.g., new image version), Kubernetes creates a new ReplicaSet and gradually replaces pods (rolling update), Can easily rollback to previous versions.

apiVersion: apps/v1
Kind: Deployment
Metadata:
  Name: nginx-deployment
Spec:
  Replicas: 3
  Selectors:
    matchLabels:
      App: nginx
  Template:
    Metadata:
      Labels:
        App: nginx
    Spec:
      Containers:
      - name: nginx
        Image: nginx:1.25

Note -> use when: you have stateless applications (web servers, APIs), you want rolling updates, rollbacks, versioning, and scaling.

3) StatefulSet->
- Purpose: manages stateful applications -pods that need stable network IDs, persistent storage, or ordered deployment. Each pod gets a unique, persistent identity (e.g., pod-0, pod-1,....). Wach pod can have its own persistentVolumeClaim (PVC), Pods are created, updated and deleted in order.

apiVersion: apps/v1
Kind: StatefulSet
Metadata:
   Name: mysql
Spec:
  serviceName: "mysql"
  Replicas: 3
  Selector:
    matchLabels:
      App: mysql
  Template:
    Metadata:
      Labels:
       App: mysql
      Spec:
        Containers:
        - name: mysql
          Image: mysql:8
          volumeMounts:
          - name: data
            mountPath: /var/lib/mysql
  volumeClaimTempletes:
  - metadata:
      Name: data
    Spec:
      accessMode: ["ReadWriteOnce"]
      Resources:
        Requests:
         Storage: 5Gi

-> use when -> Databases (MySQL, MongoDB, Cassandra, etc), Applications needing stable pod identity or storage, Ordered startup/shutdown (like cluster bootstrapping).

4) DaemonSet ->
- Purpose -> Ensures that one pod runs on every (or selected) Node in the cluster, Automatically adds a pod to new nodes that join the cluster, Removes Pods when nodes live, Often used for node-level agents or monitoring.

apiVersion: apps/v1
Kind: Daemonset
Metadata:
  Name: node-monitor
Spec:
  Selector:
    matchLabels:
     App: node-monitor
    Template:
      Metadata:
        Labels:
          App: node-monitor
      Spec:
        Containers:
        - name: node-exporter
          Image: prom/node-exporter: latest

- use when -> you need one pod per node, log collectors (Fluent, Filebeat), Monitoring agents (Node Exporter), Network plugins (CNI)

--------------------------------------------------------------

Q.23) What is the difference between Deployment and ReplicaSet?
-> ReplicaSet ->
- Purpose: Ensure a specified number of identical pods are running at any time.
- What it does: Maintains a stable set of pod replicas, if a pod fails or is deleted, the replicaSet creates a new one.
- What is doesn't do ->
- Does not support rolling updates or version management, if you change the pod template in a Replicaset, it won't automatically update existing pods - you'd have to delete and recreate them manually.

-> Deployment ->
- Purpose: provides declarative updates to pods and ReplicaSets - adds versioning, rollback, and rollout control on top of ReplicaSets.
- What it does:-
- Manages ReplicaSets automatically, Handles rolling updates (zero-downtime updates), Supports rollback to previous versions, Provides revision history and scaling.

- How it works:-
- When you create a Deployment, it creates a ReplicaSet under the hood, When you update the Deployment (e.g., change image version), it creates a new ReplicaSet, gradually replaces Pods from the old one -> new one

apiVersion: apps/v1
Kind: Deployment
Metadata:
  Name: my-deployment
Spec:
  Replicas: 3
  Selector:
    matchLabels:
      App: nginx
    Template:
      Metadata:
        Labels:
          App: nginx
      Spec:
        Containers:
          - name: nginx
            Image: nginx:1.22

In short -> ReplicaSet ensures Pod availability; Deployment ensures application lifecycle management. You almost never create ReplicaSets directly - you use Deployments instead.

----------------------------------------------------------

Q.24) What are Kubernetes Probes (Liveness, Readiness, Startup)?
-> Kubernetes Probes are health checks performed by the Kubelet (the node agent) on containers. They tell Kubernetes whether your container is: Alive, Ready to receive traffic, Still starting up. These help Kubernetes decide whether to restart, remove from service, or wait for a container.

- Types of Probes ->
1) Liveness Probe ->
Purpose:- checks if the container is alive (still running properly), if it fails, Kubernetes restarts the container, Prevents cases where an application is running but "stuck" (e.g., deadlock).

livenessProbe:
  httpGet:
    Path: /healthz
    Port: 8080
  initialDelaySeconds: 10
  periodSeconds: 5

Meaning:- After 10 seconds, K8s will check /healthz on port 8080 every 5 seconds. If it fails -> restart the container.

2) Readiness Probe ->
Purpose: Checks if the container is ready to serve requests. If it fails, Kubernetes removes the  pod from service Endpoints (traffic won't be sent). The container is not restarted, just marked as unready until it passes.

Readiness Probe:
  topSocket:
    Port: 8080
  initialDelaySeconds: 5
  periodSeconds: 5

Meaning :- After 5 seconds, check if port 8080 is open every 5 seconds. If fails -> Pod stays running but receives no traffic until it passes.

3) Startup Probe ->
Purpose: Checks if the container's applications has started successfully, useful for slow-starting apps (e.g., java, Oracle-based, or legacy apps), While the startup probe runs, liveness and readiness checks are disabled, once it succeeds, Kubernetes enables the other probes.

startupProbe:
  httpGet:
    Path: /ready
    Port: 8080
  failureThreshold: 30
  periodSeconds: 10

Meaning :- K8s will check /ready every 10s, if it fails 30 times, then the considered failed -> restarted

In Short:-
- "Liveness = Restart if unhealthy"
- "Readiness = Don't send traffic until ready"
- "Startup" wait longer before starting other checks.

---------------------------------------------------------------------

Q.25) What is the difference between Stateful and Stateless applications? Give examples.
-> 
1) Stateless Applications ->
Definition:- A stateless application does not store any data or session information between requests. Each request is independent - the server doesn't remember anything from previous interactions. If one instance goes down, another can take over easily, easy to scale horizontally (add, remove replicas), common in micro services and cloud-native design.

Example :- Web servers serving static content (e.g., Nginx, Apache), REST APIs where session info is stored in a DB token (not in memory), Frontend apps (React, Angular, etc.), Microservices calling external DBs for data.
In Kubernetes managed using a Deployment, Pods are identical and interchangeable.

2) Stateful Applications:-
A stateful application stores data, session, or client state locally or in a persistent volume, Each request depends on past data or context, Each instance maintains unique data identity, scaling and restarting require state preservation, Needs persistent storage and consistent network identity.

Example:- Databases: mysql, postgreSQL, MongoDB
Message queues: Kafka, RabbitMQ
Caching systems: Redis (in persistent mode)
File storage services.

In Kubernetes:-
Managed using statefulset
Pods have -> stable network IDs, Persistent volumes, Ordered deployment & scaling.

In short:
- "Stateless: Every request is new -> easier to scale and recover"
- "Stateful: keeps memory or data between requests -> needs stable identity and storage."

----------------------------------------------------------------------------
 
Q.26) What are Namespaces in Kubernetes?
-> A Namespace in Kubernetes is a logical partition inside a cluster, it's used to organise, isolate, and manage resources (like pods, services, deployments) within the same cluster.
Think of it as a folder inside your Kubernetes cluster - each namespace contains its own set of objects.

Purpose of Namespaces:- namespaces are designed to:

1) Group resources logically -> separate environments: dev, test, prod
2) Avoid naming conflicts-> you can have multiple Deployments named nginx - one in each namespace.
3) Apply access control (RBAC) -> Control which users or teams can access specific namespaces.
4) Set resource limits (via ResourceQuota, LimitRange) -> Example: limit CPU/memory usage for a team's namespace.

-> Create a namespace:- kubectl create namespace dev

-> List resources in a namespace:- kubectl get pods -n dev

- Namespace isolation -> Namespaces isolate resource names and access, but not networking, pods from different namespaces can still communicate, unless restricted via NetworkPolicies

-> Best Practices -> Use namespaces to separate environments (e.g., dev, qa,prod), Use RBAC and ResourceQuota for each namespace to control access and usage, Don't overuse namespace for every micro service - they're best for logical isolation, not per component separation.

In short -> A namespaces is like a virtual cluster inside your Kubernetes cluster it helps organise, isolate, and manage resources efficiently.

-----------------------------------------------------------

Q.27) What is Port Forwarding in Kubernetes?
-> port forwarding allows you to access a Pod, Service, or Deployment running inside a cluster from your local machine - without exposing it publicly. It temporarily maps a local port on your computer to a port inside a pod (or service). That means you can directly connect to the application inside Kubernetes as if it were running locally.

Why it's used -> to debug application running inside pods, to rest APIs or web apps inside the cluster without creating a service or ingress, to access internal-only pods (like databases or internal APIs).

-> kubectl port-forward <pod-name> <local-port>:<pod-port>

- Important Notes -> port forwarding is temporary - it lasts until you stop the command (Ctrl + C), it works only while your kubectl session is active, it's mainly for local development ans debugging, not production, you must have network access and permissions to the pod (via kubectl context).

- in short -> Port Forwarding lets you access a pod or service inside Kubernetes directly from your local machine by mapping local ports to pod ports - mainly used for debugging or testing without exposing the app externally.

---------------------------------------------------------------

Q.28) Which port is used by DNS (Route 53)?
-> port no 53 is used by DNS (Route 53).

----------------------------------------------------------------

Q.29) which tool is used for managing Kubernetes deployments in a CI/CD pipeline?
-> In a CI/CD pipeline, kubernetes deployments are typically managed using deployment automation tools that integrate with CI/CD platforms.
Here are the most commonly used tools for managing Kubernetes deployments:
Argo CD, Helm, Jenkins X, Tekton, Kustomize

- Typical CI/CD Flow Example
1) Developer pushes code -> GitHub/GitLab triggers pipeline
2) Jenkins/Tekton builds and tests the application.
3) Docker image pushed to registry.
4) Deployment manifests (Helm/Kustomize) updated in Git.
5) Argo CD or Jenkins deploys changes to Kubernetes cluster.

---------------------------------------------------------------

Q.30) what is docker swarm?
-> docker swarm is a container orchestration tools. 

----------------------------------------------------------------------

Q.31) How do you implemented high availability and disaster recovery for RDS across regions?
-> AWS RDS (Relational Database Service) provides multiple features to ensure both High Availability (HA) and Disaster Recovery (DR), but they serve different purposes:

High Availability -> Keep the database running within the same region during AZ failures.
Disaster Recovery (DR) -> Protect against entire failures - ensure cross-region recovery.

1) High Availability (Within the Same Region)
- Multi-AZ Deployment
- When you enable Multi-AZ, RDS automatically creates a synchronous standby replica in a different Availability Zone (AZ) within the same region.
- AWS Automatically handles :- Failover (in case the primary instance fails), Data replication (synchronous), DNS endpoint switching.

2) Disaster Recovery (Across Regions) ->
- Cross-Region Read Replica (Asynchronous Replication) ->
- you can create a Read Replica in different AWS region, Replication is asynchronous, so there may be a small lag, you can promote the replica to a standalone database in case of a regional disaster.

- In short ->
- multi-AZ (HA):- protects against AZ failure - synchronous standby".
- Cross-Region Read Replica (DR) :- protects against region failure - async replication + manual promotion.
- Cross-region snapshots: cost-effective backup-based DR.
- Use Route53 failover routing and ClowdWatch alerts for automation.

---------------------------------------------------------------------

Q.32) Explain the process to perform blue/green deployments in AWS using EC2 or ECS?
-> here's clear breakdown of how Blue/Green Deployments work in AWS, using both EC2 (with Elastic Load Balancer) and ECS (with CodeDeploy or ECS services).

- Blue/Green Deployment is a deployment strategy that minimises downtime and risk by running two separate environments:
- Blue: The current live (production) environment.
- Green: The new version of the application to be deployed.
Once the green environment is verified to work correctly, traffic is switched from Blue -> Green. If something goes wrong, you can rollback instantly by switching traffic back to blue.

1) Blue/Green Deployment on EC2 (using ELB + Auto Scaling + CodeDeploy)
Architecture Overview
- Blue environment: Current EC2 instances behind an Elastic Load Balancer (ELB)
- Green environment: New Ec2 instances (new version of app) behind the same ELB or a new ELB.
- AWS CodeDeploy: manages the switching and rollback

-------------------------------------------------------------------------
 
Q.33) what are the best practices for scaling EKS workloads in production?
-> Scaling EKS (Amazon Elastic Kubernetes Services) workloads efficiently in production in one of the key responsibilities of a DevOps/SRE engineer.
Here's comprehensive breakdown of best practices for scaling EKS workloads.
1) Use the right Autoscaling Components.
2) Configure Horizontal Pod Autoscaler (HPA) Properly.
3) Use Cluster Autoscaler (CA) or Carpenter for Node Scaling.
4) Use Vertical Pod Autoscaler (VPA) for Resource Optimisation.
5) Optimise Node types Costs.
6) Monitor and Tune Scaling.
7) Use Multiple Node Groups (Workload Segmentation).
8) Implement Rolling Updates and Surge Controls.
9) Leverage AWS Tools and Add-ons.
10) Regularly Review and Load Test.

----------------------------------------------------------------------

Q.34) How do you monitor and troubleshoot network latency issues between VPCs across regions?
-> Monitoring and troubleshooting network latency between VPCs across AWS regions requires both visibility (metrics + traces) and diagnostic tools to pinpoint where the delay occurs - whether it's DNS, routing, or inter-region links.

1) Understand the Architecture ->
When VPC's are in different AWS regions, they typically communicate through:
- VPC Peering
- AWS Transit Gateway (TGW)
- AWS Private Link
- Public endpoints (via Internet Gateway or NAT)
Each path introduces different latency characteristics - for example, VPC peering is faster than public routing, but still subject to inter-region distance.

2) Continuous Monitoring: Measure Baseline Latency
3) Use Network-Level Diagnostic Tools
4) Troubleshooting Steps ->
- Step 1 -> Verify Connectivity
- Step 2 -> Check VPC Peering or TGW Steps
- Step 3 -> Analyse Latency Trends
- Step 4 -> Check Inter-Region AWS Latency
- Step 5 -> Run Application-Level Tests
- step 6 -> Best Practices to Minimise and Monitor Latency

---------------------------------------------------------

Q.35) what are the steps to securely connect on-perm infrastructure to AWS?
-> To securely connect on-premises infrastructure to AWS, organization usually use VPN or Direct Connect with proper security controls. Below are the common steps and architecture approach used in real environments.

1. Create a VPC in AWS

First create a Amazon Virtual Private Cloud (VPC).

Steps:

Define CIDR range (example: 10.0.0.0/16)

Create public and private subnets

Configure route tables

Attach Internet Gateway (if needed)

This VPC will host your AWS resources.

2. Create a Virtual Private Gateway

Create a **AWS Virtual Private Gateway and attach it to the VPC.

Purpose:

Acts as the VPN endpoint on AWS side

Allows secure communication between AWS and on-prem network.

3. Configure Customer Gateway

Create a AWS Customer Gateway.

Provide:

Public IP of your on-prem firewall/router

BGP ASN (if using dynamic routing)

This represents the on-premises gateway device.

4. Create Site-to-Site VPN Connection

Create **AWS Site-to-Site VPN between:

Virtual Private Gateway (AWS)

Customer Gateway (on-prem)

Features:

IPsec encrypted tunnel

Two tunnels for high availability

After creation AWS provides:

VPN configuration file for your router (Cisco, Fortigate, Palo Alto etc.)

5. Configure On-Prem Firewall / Router

On your on-premises device:

Configure IPsec tunnel

Use parameters from AWS config file

Configure IKE, encryption algorithms

Enable BGP or static routing

This establishes secure tunnel connectivity.

6. Update Route Tables

Update AWS route tables so traffic goes through VPN.

Example:

Destination: 192.168.1.0/24
Target: Virtual Private Gateway

This ensures on-prem traffic routes via VPN.

7. Configure Security Controls

Add additional security layers:

AWS Security Groups → Instance-level firewall

AWS Network ACL → Subnet-level firewall

Restrict access to only required ports.

8. Enable Monitoring & Logging

Use monitoring services:

Amazon CloudWatch → VPN tunnel metrics

AWS CloudTrail → Audit API activity

Enable VPC Flow Logs for network monitoring.

9. Optional: Use AWS Direct Connect (For Enterprise)

For high bandwidth and low latency, companies use:

AWS Direct Connect

Benefits:

Dedicated private connection

More stable than internet VPN

Often combined with VPN for encryption

✅ Typical Architecture

On-Prem Data Center
      |
Firewall / Router
      |
IPSec VPN Tunnel
      |
Virtual Private Gateway
      |
AWS VPC
   |        |
Private   Public
Subnet    Subnet

💡 Interview Tip (Short Answer – 3 lines)
To securely connect on-prem infrastructure to AWS, we create a VPC, attach a Virtual Private Gateway, and configure a Customer Gateway representing the on-prem router. Then we establish an IPSec Site-to-Site VPN and update route tables. Additional security is enforced using Security Groups, NACLs, and monitoring with CloudWatch.

----------------------------------------------------------------------------
Q.36) How do you handle IP exhaustion in a VPC and what are your options?
-> Ip exhaustion in a Amazon  Virtual Private Cloud (VPC) happens when all available private private IPs in the VPC or its subnets are used. To handle this, there are sevral practical options used in production environments.

1. Expand the VPC CIDR Block

You can add additional CIDR ranges to the VPC using Amazon VPC CIDR block association.

Example:

Existing VPC CIDR → 10.0.0.0/16

Add another CIDR → 10.1.0.0/16

Benefits:

Expands available IP address pool

No need to recreate VPC

Existing resources remain unaffected

2. Create New Subnets with the New CIDR

After adding CIDR:

Create new subnets

Move or deploy new workloads there

This is the most common production approach.

3. Use Secondary CIDR Blocks for Specific Workloads

You can allocate a secondary CIDR to isolate workloads.

Example:

10.0.0.0/16 → existing workloads

10.2.0.0/16 → Kubernetes or new services

Often used with Amazon Elastic Kubernetes Service clusters.

4. Use IPv6

Enable dual-stack networking.

Advantages:

Practically unlimited IP space

Reduces pressure on IPv4

AWS supports IPv6 inside **Amazon Virtual Private Cloud.

5. Use Private NAT or Shared Services

Instead of assigning IPs to many instances, use centralized services such as:

AWS NAT Gateway

Shared load balancers

Proxies

This reduces the number of required IP allocations.

6. Re-architect with Multiple VPCs

Create additional VPCs and connect them using:

AWS VPC Peering

AWS Transit Gateway

Useful when:

CIDR expansion is not possible

Large microservices architecture exists

7. Clean Up Unused Resources

Often IP exhaustion occurs due to unused resources:

Check and remove:

Unused Elastic Network Interfaces

Stopped instances holding IPs

Old load balancers

Idle container tasks

✅ Short Interview Answer (2–3 lines)
If IP exhaustion occurs in a VPC, we can add a secondary CIDR block and create new subnets to expand the address space. Other options include enabling IPv6, cleaning unused ENIs, or creating additional VPCs connected via VPC Peering or Transit Gateway.

------------------------------------------------------------------------------

Q.37) Explain how you would automate patching for EC2 instances across multiple accounts?
-> To automate patching of EC2 instances across multiple accounts, you can use centralized patch management with AWS Systems Manager and cross-account automation.

1. Use AWS Systems Manager Patch Manager

Use AWS Systems Manager Patch Manager to automate patching of instances.

Features:

Automates OS patching for Linux and Windows

Allows defining patch baselines

Supports scheduled patching

Ensure the SSM agent is installed on all EC2 instances.

2. Enable Systems Manager Across Accounts

Use AWS Systems Manager with a central management account.

Steps:

Enable AWS Organizations integration

Register all member accounts

Use a centralized patching strategy

This allows managing patching from a single account.

3. Create a Custom Patch Baseline

Define rules using AWS Systems Manager Patch Baseline.

Example rules:

Install critical and security updates

Approve patches automatically after X days

Reject problematic patches

This ensures consistent patch policy across accounts.

4. Use Patch Groups

Tag instances using patch groups.

Example tag:

PatchGroup = production-linux
PatchGroup = staging-windows

Patch Manager uses these tags to apply correct patch baselines.

5. Automate with Maintenance Windows

Use AWS Systems Manager Maintenance Windows to schedule patching.

Example:

Sunday 02:00 AM

Run patch scan and install

Tasks executed:

Patch scanning

Patch installation

Reboot if required

6. Cross-Account Execution

For multiple accounts:

Use AWS Organizations

Share patch policies

Use IAM roles for cross-account access

This enables centralized automation.

7. Monitoring and Compliance

Track patch compliance using:

AWS Systems Manager Compliance

Amazon CloudWatch

You can generate reports to see:

Missing patches

Failed patch installations

Non-compliant instances

✅ Short Interview Answer (3–4 lines)
To automate patching across multiple accounts, I use AWS Systems Manager Patch Manager integrated with AWS Organizations for centralized management. I create custom patch baselines, assign instances using patch group tags, and schedule patching using maintenance windows. Monitoring and compliance are tracked through Systems Manager Compliance and CloudWatch.

-------------------------------------------------------------------------

Q.38) How do you enforce compliance and governance in AWS organisations across accounts?
-> To enforce compliance and governance across multiple AWS accounts, organizations usually use centralized control provided by AWS Organizations, policies, and monitoring services.

1. Use AWS Organizations for Centralized Account Management

Use AWS Organizations to manage multiple AWS accounts under a single organization.

Key features:

Create Organizational Units (OUs) such as Dev, Test, and Prod

Apply governance policies at the OU or account level

Centralized billing and account control

2. Apply Service Control Policies (SCPs)

Use AWS Service Control Policies to restrict what accounts can do.

Examples:

Deny creation of resources outside approved regions

Prevent disabling logging services

Restrict usage of certain services

Example policy idea:

Deny EC2 creation outside approved region

This enforces organization-wide security rules.

3. Implement AWS Config for Compliance Monitoring

Use AWS Config to track configuration changes and compliance.

Capabilities:

Detect non-compliant resources

Use Config Rules to enforce policies

Example rules:

S3 buckets must not be public

EBS volumes must be encrypted

Security groups should not allow open SSH

4. Use AWS Control Tower for Governance

Many enterprises deploy AWS Control Tower.

It provides:

Preconfigured governance framework

Guardrails (preventive and detective controls)

Automated account provisioning

5. Centralized Logging and Auditing

Enable logging across all accounts using:

AWS CloudTrail → API audit logs

Amazon CloudWatch → monitoring and alerts

AWS Security Hub → centralized security findings

Logs are usually stored in a central security account.

6. Enforce Security Standards

Use security services like:

Amazon GuardDuty → threat detection

AWS IAM → enforce least privilege access

AWS Config Conformance Packs → standard compliance frameworks (CIS, PCI, etc.)

7. Use Tag Policies and Resource Standardization

Use AWS Tag Policies to enforce consistent tagging.

Example required tags:

Environment

Owner

CostCenter

This helps with cost tracking and governance.

✅ Short Interview Answer (3–4 lines)
To enforce compliance across accounts, I use AWS Organizations with Service Control Policies to restrict actions at the organization level. I implement AWS Config rules and Conformance Packs for compliance monitoring, while CloudTrail and Security Hub provide centralized logging and security visibility. For enterprise governance, AWS Control Tower is often used to apply guardrails and standardized account setups.

------------------------------------------------------------------

Q.39) what are the different ways to control egress traffic from a private subnet in AWS?
-> There are several ways to control egress (outbound) traffic from a private subnet in Amazon Virtual private Cloud. These methods combine routing, firewalls, and policy controls.
1. Use NAT Gateway

Instances in a private subnet can access the internet through a AWS NAT Gateway.

How it works:

Private subnet route table sends 0.0.0.0/0 traffic to NAT Gateway.

NAT Gateway is placed in a public subnet.

Instances remain private but can access external services.

Control options:

Restrict traffic using security groups and NACLs.

2. Use NAT Instance with Firewall Rules

Instead of NAT Gateway, you can use a NAT Instance.

Advantages:

Install firewall tools (iptables, proxy)

Filter outbound traffic by:

IP

Port

Domain

This gives more granular control but requires maintenance.

3. Use Network ACLs

Use AWS Network ACL at the subnet level.

Capabilities:

Allow or deny outbound ports

Control destination IP ranges

Example:

Allow HTTPS outbound

Deny all other ports

4. Control with Security Groups

Use AWS Security Groups attached to instances.

Examples:

Allow outbound only on port 443

Block all other outbound connections

Security groups act as stateful firewalls.

5. Use VPC Endpoints to Avoid Internet Access

Use AWS VPC Endpoint for AWS services.

Example:

Access S3 or DynamoDB privately

No internet access required

Benefits:

Secure internal AWS communication

Reduced attack surface

6. Use AWS Network Firewall

For enterprise environments use AWS Network Firewall.

Features:

Deep packet inspection

Domain filtering

URL-based egress control

Centralized firewall policies

Common in regulated environments.

7. Use Proxy Servers for Egress Control

Organizations deploy:

HTTP/HTTPS proxy

Squid proxy

Traffic flow:

Private Instances → Proxy → Internet

Allows:

Domain-based filtering

Logging of outbound traffic.

✅ Short Interview Answer (2–3 lines)
Egress traffic from a private subnet can be controlled using NAT Gateway or NAT instances, combined with security groups and network ACLs to restrict outbound ports and destinations. For tighter control, organizations use VPC endpoints to avoid internet access and AWS Network Firewall or proxy servers for centralized egress filtering.

---------------------------------------------------------------------

Q.40) Describe the steps to build an auto-healing infrastructure in AWS?
-> Building an auto-healing infrastructure in AWS means the system can automatically detect failures and recover without manual intervention. The typical approach combines monitoring, scaling, and automation.

1. Use Auto Scaling Groups

Deploy instances using Amazon EC2 Auto Scaling instead of standalone instances.

How it helps:

Automatically replaces unhealthy instances

Maintains desired instance count

Supports scaling policies

Example:

Desired capacity: 3
If 1 instance fails → Auto Scaling launches a new instance automatically
2. Implement Health Checks

Use health checks to detect failures.

Types:

EC2 status checks

Application health checks via load balancer

Use Elastic Load Balancing to perform application-level health checks and route traffic only to healthy instances.

3. Use CloudWatch Monitoring and Alerts

Use Amazon CloudWatch to monitor system metrics.

Examples:

CPU utilization

Memory usage

Application errors

Disk usage

Set alarms to trigger automated actions.

4. Automated Recovery Actions

Configure CloudWatch alarms to trigger recovery.

Example actions:

Restart EC2

Replace instance

Scale infrastructure

Use AWS Lambda functions to perform automated remediation tasks.

Example:

CloudWatch Alarm → Lambda → Restart service / recreate instance
5. Use Multi-AZ Architecture

Deploy resources across multiple availability zones using Amazon EC2.

Benefits:

Automatic failover

High availability

Reduced impact of AZ failures

6. Automate Configuration with Infrastructure as Code

Use tools like:

AWS CloudFormation

Terraform

This ensures:

Fast recreation of infrastructure

Consistent configuration

7. Use Systems Manager for Self-Healing Tasks

Use AWS Systems Manager automation documents.

Example tasks:

Restart failed services

Apply patches

Run remediation scripts

8. Implement Database and Storage Resilience

Use resilient managed services such as:

Amazon RDS with Multi-AZ

Amazon S3 for durable storage

These services automatically handle failures.

✅ Short Interview Answer (3–4 lines)
To build auto-healing infrastructure in AWS, I deploy applications using EC2 Auto Scaling Groups behind Elastic Load Balancers with health checks. CloudWatch alarms detect failures and trigger automated remediation using Lambda or Systems Manager. Infrastructure is defined using Terraform or CloudFormation, and resources are deployed across multiple availability zones for high availability.

------------------------------------------------------------------------

Q.41) How would you troubleshoot intermittent packet drops between EC2 instances in the same AZ?
-> If there are intermitten packet drops between EC2 instances in the same Availability Zone, i would troubleshoot it layer by layer - from network configuration to OS and application level.

1. Verify Security Groups

Check AWS Security Groups attached to both instances.

Things to verify:

Inbound rules allow required ports/protocols

Outbound rules are not overly restrictive

Correct security group referencing (SG-to-SG rules)

Example:

Allow TCP/ICMP between instance security groups
2. Check Network ACLs

Review AWS Network ACL for the subnet.

Important checks:

Stateless rules allow both request and response traffic

Ephemeral ports (1024–65535) are allowed

No deny rules causing intermittent blocking

3. Validate Route Tables

Ensure routing is correct inside Amazon Virtual Private Cloud.

For instances in the same subnet:

Local route should exist (VPC CIDR → local)

No misconfigured routes sending traffic elsewhere.

4. Check Instance-Level Firewall

Verify OS firewall rules.

Examples:

iptables

firewalld

ufw

Run checks:

iptables -L

Sometimes packet drops happen due to host firewall rules.

5. Monitor Network Metrics

Check instance metrics using Amazon CloudWatch.

Look for:

Network packets dropped

High network utilization

CPU saturation

Example metrics:

NetworkPacketsIn

NetworkPacketsOut

6. Enable VPC Flow Logs

Use Amazon VPC Flow Logs to analyze traffic.

You can identify:

ACCEPT / REJECT traffic

Source/destination IP

Ports and protocols

This helps detect network-level filtering.

7. Check ENI or Instance Limits

Each instance has limits on Elastic Network Interfaces and bandwidth.

Review Amazon Elastic Network Interface usage.

Issues may occur due to:

Bandwidth limits

High PPS (packets per second)

8. Perform Network Diagnostics

Run network tests between instances.

Examples:

ping <private-ip>
mtr <private-ip>
traceroute <private-ip>

Or use AWS Reachability Analyzer to identify path issues.

9. Check Application-Level Issues

Sometimes packet drops are caused by:

Application connection limits

Load balancer health checks

TCP connection resets

Check application logs for errors.

✅ Short Interview Answer (3–4 lines)
To troubleshoot intermittent packet drops between EC2 instances in the same AZ, I first verify security groups, network ACLs, and route tables inside the VPC. Then I analyze traffic using VPC Flow Logs and CloudWatch metrics to identify drops or bandwidth limits. I also check instance-level firewalls and network diagnostics tools like ping or mtr to isolate the issue.

----------------------------------------------------------------------------------

Q.42) Explain the role of Transit Gateway in large-scale AWS networks and its limitations?
-> AWS Transit Gateway is a centralized networking service used to connect multiple VPCs, on-premises networks, and VPN connections in lagre AWS environments. it acts as a hub that simplifies network architecture and reduces the complesity of managing many connections.

Role of Transit Gateway in Large-Scale AWS Networks
1. Centralized Network Hub

Transit Gateway acts as a hub-and-spoke architecture.

Example:

VPC-A
   \
VPC-B ---- Transit Gateway ---- On-Prem Data Center
   /
VPC-C

Instead of creating multiple AWS VPC Peering connections between every VPC, all VPCs connect to the Transit Gateway.

Benefits:

Simplifies network management

Reduces connection complexity

2. Connect Multiple VPCs at Scale

In large organizations using Amazon Virtual Private Cloud, there may be dozens or hundreds of VPCs across accounts.

Transit Gateway allows:

Centralized connectivity

Easy addition of new VPCs

Shared networking services

3. Hybrid Cloud Connectivity

Transit Gateway can connect AWS with on-prem infrastructure using:

AWS Site-to-Site VPN

AWS Direct Connect

This enables a single gateway for hybrid networking.

4. Inter-Region Connectivity

You can connect Transit Gateways in different regions using Transit Gateway Peering.

Use cases:

Disaster recovery

Global architectures

5. Route Table Segmentation

Transit Gateway supports multiple route tables, allowing network segmentation.

Example:

Dev VPCs communicate only with Dev resources

Production VPCs are isolated

This improves security and governance.

Limitations of Transit Gateway
1. Cost

Transit Gateway can become expensive because charges apply for:

Hourly attachment cost

Data processing per GB

For smaller environments, VPC Peering may be cheaper.

2. Bandwidth Limits

Each attachment has bandwidth limitations. High-throughput workloads may require careful design and scaling.

3. Regional Service

Transit Gateway operates within a region.
For cross-region connectivity you must configure Transit Gateway peering, which adds complexity.

4. Route Table Complexity at Scale

In very large networks:

Route tables can become complex

Requires careful route management and segmentation

5. No Native Security Filtering

Transit Gateway itself does not provide firewall capabilities.

You often need additional services like:

AWS Network Firewall

Third-party firewalls

✅ Short Interview Answer (3–4 lines)
AWS Transit Gateway provides a centralized hub to connect multiple VPCs and on-prem networks, enabling scalable hub-and-spoke architectures instead of complex VPC peering meshes. It simplifies hybrid connectivity using VPN or Direct Connect and supports route segmentation. However, it has limitations such as additional cost, regional scope, bandwidth considerations, and lack of built-in firewall capabilities.

----------------------------------------------------------------------------------------------

Q.43) What is the difference between NLB, ALB, and Gateway Load Balancer? When to use each?
-> ALB (Application Load Balancer) -> This is a load balancer that routes traffic to target groups based on the content of the request. It operates at the application layer (layer 7 in the OSI model) and supports HTTP, HTTPS, and WebSocket traffic. ALB is designed to handle high levels of traffic and provides advanced request routing features, such as support for multiple protocols, path-based routing, and ability to bind to multiple SSL certificates.

Limitation:
- Application LB per region - 20
- Target groups - 3000

Load Balancer Components Limits:
- Listeners - 50
- Targets load balancer - 1000
- Subnet per Availability zone - 1
- Rules - 100
- Security groups - 5

- NLB (Network Load Balancer) -> This is a load balancer that routes traffic to target groups based on IP address and TCP port. It operates at the network layer (layer 4 in the OSI model) and supports any TCP-based protocol. NLB is designed to handle high levels of traffic and provides ultra-low latencies.

- Limitation:

- Network LB per region - 20
- Target groups - 3000

Load Balancer Components Limits:

- Listeners - 50
- Targets per availability zone with cross-zone load balancing disabled - 200
- Targets per availability zone with cross-zone load balancing enabled - 200
- Subnet per Availability zone - 1
- Target per availability zone with cross-zone load  

-----------------------------------------------------------------

Q.44) How would you secure access to AWS infrastructure for third-party vendors or consultants?
-> Securing access for external vendors or consultants requires a strict, controlled, and auditable approach. The key is to provide least-privilege, time-bound, and monitored access using securing identity and network controls.
- Use IAM Roles + Temporary Credentials 
- Enforce Strong Authentication
- Use AWS identity Center (SSO)
- Implement Network Access Controls
- Use just-in-Time (JIT) Access
- Isolate Vendor Environment
- Implement Logging & Monitoring
- Validate Vendor Devices
- Use key Management & Secrets Managements
- Formal Onboarding & Offboarding.

--------------------------------------------------------------

Q.45) How do you manage secrets securely in a CI/CD pipeline using AWS services?
-> - Use AWS Secrets manager or SSM Parameter Store
- Grant Latest-privilege Access to the CI/CD Pipeline
- Fetch Secrets Dynamically During the Build/Deploy Stage.
- Use Encryption & key management (KMS)
- Avoid Storing Secrets in Build Logs
- Rotate Secrets Regularly
- Isolate Secrets by Environment
- Use OIDC for Github/GitLab Access (No Access Keys)
- Enable Logging & Monitoring.

--------------------------------------------------------------------

Q.46) Explain a scenario where Route 53 latency-based routing caused issues and how to fix it?
-> Amazon Route 53 latency-based routing directs users to the AWS region with the lowest network latency based on measurments between the user's DNS resolver and AWS regions. while useful for global applications, it can sometimes cause unexpected routing problems.

Example Scenario Where Latency-Based Routing Caused Issues
Situation

A company deployed its application in two regions:

Mumbai (ap-south-1)

Singapore (ap-southeast-1)

They configured latency-based routing in Route 53 so users would automatically connect to the fastest region.

Problem observed:

Some India-based users were routed to Singapore instead of Mumbai

Users experienced higher latency and slower response times

Application logs showed traffic going to the wrong region intermittently

Root Cause

Latency-based routing uses the latency between the user's DNS resolver and AWS regions, not the user’s actual location.

Example:

User → ISP DNS Resolver → Route 53

If the ISP DNS resolver is located in another city or region:

Route 53 calculates latency from that resolver

It may incorrectly select another AWS region

Example:

User in Mumbai
↓
DNS Resolver in Singapore
↓
Route 53 thinks Singapore region is faster
Impact

Users routed to non-optimal region

Higher response times

Uneven traffic distribution

Increased cross-region data transfer cost

How to Fix the Issue
1. Use Latency Routing with Health Checks

Configure Route 53 health checks so if the primary region is degraded, traffic shifts automatically.

This prevents routing to unhealthy endpoints.

2. Combine with Geolocation Routing

Instead of relying only on latency, combine policies.

Example:

India → Mumbai region

Southeast Asia → Singapore region

This reduces incorrect routing.

3. Use Route 53 Traffic Flow

Use Route 53 Traffic Flow policies to create more advanced routing logic.

You can combine:

Latency

Failover

Geolocation

4. Use AWS Global Accelerator

For critical applications, use AWS Global Accelerator.

Benefits:

Uses AWS global edge network

Routes users based on actual network performance

Provides more accurate traffic routing than DNS-based latency routing.

5. Monitor with CloudWatch

Track routing behavior and endpoint health using Amazon CloudWatch metrics and logs.

✅ Short Interview Answer (3–4 lines)
A common issue with Route 53 latency-based routing occurs when DNS resolvers are located in different regions than the user, causing traffic to be routed to the wrong AWS region. This can increase latency and affect user experience. The fix is to combine latency routing with geolocation routing, health checks, or AWS Global Accelerator to ensure traffic goes to the most appropriate region.

-------------------------------------------------------------------

Q.47) what are VPC endpoints and when would you use interface vs gateway endpoints?
-> VPC Endpoints allow resources inside a Amazon Virtual Private Cloud (VPC) to privately connect to AWS services without using the internet, NAT Gateway, or VPN. Traffic stays within the AWS network, improving security and reducing latency.
There are two main types: Gateway Endpoints and Interface Endpoints.

1. Gateway Endpoints

A Gateway Endpoint is used for specific AWS services and works at the route table level.

Supported services:

Amazon S3

Amazon DynamoDB

How it works

A route is added in the route table directing traffic to the endpoint.

Example:

Destination: S3 prefix list
Target: Gateway Endpoint
Characteristics

Free of cost

Highly scalable

No ENI created

Configured through route tables

Example Use Case

Private EC2 instances need to access S3 for:

Backups

Logs

Application artifacts

Without gateway endpoint, traffic would go:

EC2 → NAT Gateway → Internet → S3

With gateway endpoint:

EC2 → VPC Endpoint → S3 (private AWS network)
2. Interface Endpoints

An Interface Endpoint uses AWS PrivateLink and creates an Elastic Network Interface (ENI) inside your subnet.

Used for many AWS services such as:

AWS Systems Manager

**Amazon EC2 API

Amazon CloudWatch

AWS Secrets Manager

How it works

Creates private IP addresses in your subnet

Applications connect using private DNS

Example traffic flow:

EC2 → Interface Endpoint ENI → AWS Service
Characteristics

Uses ENI in the subnet

Supports private DNS

Hourly + data processing cost

Example Use Case

Private EC2 instances need access to:

Systems Manager

CloudWatch

Secrets Manager

without internet connectivity.

Key Differences
Feature	Gateway Endpoint	Interface Endpoint
Supported Services	S3, DynamoDB	Most AWS services
Cost	Free	Hourly + data cost
Network Component	Route table entry	ENI in subnet
Technology	VPC routing	PrivateLink
DNS	No special DNS needed	Uses Private DNS
When to Use Each
Use Gateway Endpoint when:

Accessing S3 or DynamoDB

Want free private connectivity

High throughput workloads

Use Interface Endpoint when:

Accessing other AWS services

Need PrivateLink connectivity

Want private access to API endpoints

✅ Short Interview Answer (2–3 lines)
VPC Endpoints allow private access to AWS services without using the internet. Gateway Endpoints are used for S3 and DynamoDB and work via route tables with no additional cost. Interface Endpoints use PrivateLink and ENIs to privately connect to most AWS services and support private DNS but have hourly charges.

----------------------------------------------------------------------------

Q.48) Describe how you would troubleshooting high CPU utilisation on a critical EC2 instance?
-> When troubleshooting high CPU utilization on a critical EC2 instance, the goal is to quickly identify whether the issue is caused by application load, system processes, or infrastructure limits.

1. Check CloudWatch Metrics

Start by reviewing CPU metrics in Amazon CloudWatch for the **Amazon EC2 instance.

Look for:

CPU utilization spikes

Time when the spike started

Correlation with deployments or traffic increases

Example metrics:

CPUUtilization

CPU Credit Balance (for burstable instances)

2. Identify Top CPU Processes

Login to the instance and check which process is consuming CPU.

Commands:

top

or

htop

Also check:

ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%cpu | head

This helps identify runaway processes or heavy applications.

3. Check System Load

Review system load averages.

uptime

Compare load average with the number of CPU cores.

Example:

Load average > CPU cores → CPU bottleneck
4. Check Application Logs

Look for issues in application logs such as:

Infinite loops

Heavy queries

Error retries

Memory leaks causing CPU spikes

Logs may be stored in:

/var/log

Application-specific directories

Amazon CloudWatch Logs

5. Verify Traffic or Workload Increase

Sometimes CPU spikes happen due to increased traffic.

Check:

Load balancer metrics from Elastic Load Balancing

Request rate

API usage

6. Check for Resource Contention

Investigate if CPU contention is caused by:

Background jobs

Cron jobs

Batch processing

Container workloads

Commands:

crontab -l
7. Verify Instance Type Limits

Some instances like burstable instances may run out of CPU credits.

Example instance types:

t2

t3

Check CPU credits in **Amazon CloudWatch.

If credits are exhausted, performance drops.

8. Immediate Mitigation

For critical systems, quick mitigation steps include:

Restart problematic services

Kill high CPU processes

Scale horizontally using Amazon EC2 Auto Scaling

Upgrade to larger instance type

9. Long-Term Solution

Implement improvements like:

Auto scaling

Application optimization

Caching layers

Performance tuning

✅ Short Interview Answer (3–4 lines)
To troubleshoot high CPU utilization on an EC2 instance, I first analyze CloudWatch metrics to identify when the spike started. Then I log into the instance and use tools like top, htop, and ps to find the processes consuming CPU. I also check application logs, traffic patterns, and CPU credit balance for burstable instances. Based on findings, I mitigate by restarting services, optimizing applications, or scaling the instance.

----------------------------------------------------------------------------

Q.49) What are the pros and cons of using spot instance in production workloads?
-> Amazon CE2 Spot Instances allow you to use unused EC2 capacity at a significantly lower price compared to On-Demand instances. They are useful for cost optimization but come with certain limitations, especially for production workloads.

Pros of Using Spot Instances
1. Significant Cost Savings

Spot Instances can be up to 70–90% cheaper than standard Amazon EC2 On-Demand instances.

Example:

On-Demand → $1/hour

Spot → $0.10–$0.30/hour

This is very beneficial for large-scale workloads.

2. Ideal for Fault-Tolerant Workloads

Spot Instances work well for workloads that can handle interruptions.

Examples:

Batch processing

Big data analytics

CI/CD pipelines

Containerized workloads

Many teams run Kubernetes clusters like Amazon Elastic Kubernetes Service using spot nodes.

3. Easy Integration with Auto Scaling

Spot Instances can be used with Amazon EC2 Auto Scaling.

Features:

Mixed instance policies

Automatic replacement of interrupted instances

Flexible instance types

4. Good for Stateless Applications

Stateless microservices behind Elastic Load Balancing can safely run on Spot Instances because requests can shift to other instances.

Cons of Using Spot Instances
1. Instances Can Be Terminated Anytime

AWS can reclaim Spot Instances with 2 minutes' notice if capacity is needed.

This can disrupt applications if they are not designed for interruption.

2. Not Suitable for Critical Stateful Workloads

Workloads that require persistent storage or continuous uptime are not ideal.

Examples:

Databases

Critical backend systems

Long-running tasks without checkpointing

3. Capacity Availability Can Fluctuate

Spot capacity varies based on demand in an AWS region or instance type.
This can lead to instance launch failures.

4. Requires Architecture Changes

To use Spot safely, applications must support:

Graceful shutdown

Checkpointing

Auto-scaling recovery

This adds architectural complexity.

Best Practice in Production

Most companies use hybrid compute models:

Example architecture:

Baseline workload → On-Demand instances
Scalable workload → Spot Instances

Using Auto Scaling mixed instance policies ensures stability while optimizing cost.

✅ Short Interview Answer (3–4 lines)
Spot Instances provide major cost savings (up to 90%) and are ideal for fault-tolerant and stateless workloads such as batch jobs or containerized applications. However, they can be terminated with a 2-minute notice and capacity may not always be available. In production, organizations usually combine On-Demand and Spot instances with Auto Scaling to balance reliability and cost.

---------------------------------------------------------------------------

Q.50) Explain how AWS shield and WAF integrate with a global application hosted on CloudFront.
-> For a global application, Amazon CloudFront, AWS Shield, and AWS WAF  work together to provide DDoS protection and application-layer security at the edge before traffic reaches your backend infrasturucture.

1. Architecture Overview

Typical global architecture:

Users → CloudFront → WAF → Shield → Origin (ALB / EC2 / S3)

Components:

CloudFront – CDN that serves content globally from edge locations

WAF – Filters malicious HTTP/HTTPS requests

Shield – Protects against network and transport layer DDoS attacks

This ensures attacks are blocked at the edge rather than reaching your backend.

2. Role of CloudFront

CloudFront acts as the entry point for global traffic.

Benefits:

Global edge locations reduce latency

Traffic is distributed across AWS edge network

Hides origin servers (EC2, ALB, or S3)

This reduces direct exposure of backend resources.

3. How AWS Shield Protects the Application

AWS Shield provides DDoS protection.

Shield Standard (enabled automatically)

Protects against:

SYN floods

UDP reflection attacks

Layer 3/4 DDoS attacks

Protection is automatically applied to CloudFront distributions.

Shield Advanced

Provides:

Advanced DDoS detection

Real-time attack visibility

Integration with AWS DDoS response team

4. Role of AWS WAF

AWS WAF protects the application at Layer 7 (HTTP/HTTPS).

It filters requests based on rules such as:

Examples:

Block SQL injection

Prevent XSS attacks

Rate-limit suspicious IPs

Block malicious bots

Typical WAF rule example:

Block requests if:
- Request rate > threshold
- Suspicious user agents
- Known malicious IPs

WAF rules are attached directly to the CloudFront distribution.

5. Traffic Filtering Flow

Request flow with security layers:

1. User request hits CloudFront edge location
2. AWS Shield checks for DDoS attack patterns
3. AWS WAF evaluates request rules
4. If allowed → forwarded to origin (ALB/EC2/S3)
5. If blocked → request denied at edge

This reduces load on backend servers.

6. Benefits of This Integration
Global Security

Protection happens at 200+ CloudFront edge locations.

Reduced Origin Load

Malicious traffic is blocked before reaching backend.

Low Latency

Security checks happen close to the user.

Scalability

AWS automatically scales protection during attacks.

7. Monitoring and Visibility

Security events can be monitored using:

Amazon CloudWatch

WAF logs

Shield Advanced dashboards

This helps track attack patterns and blocked traffic.

✅ Short Interview Answer (3–4 lines)
In a global application, CloudFront acts as the edge entry point, while AWS Shield protects against Layer 3/4 DDoS attacks and AWS WAF filters Layer 7 HTTP threats like SQL injection or bots. WAF rules are attached to the CloudFront distribution so malicious traffic is blocked at the edge before reaching the origin such as ALB or EC2. This architecture improves both security and performance for global applications.

-----------------------------------------------------------------------------

Q.51) How do you perform zero-downtime upgrades for a state; application hosted on ECS or EKS?
-> Performing zero-downtime upgrades for a stateful applications on Amazon Elastic Container Service (ECS) or 
Amazon Elastic Kubernetes Service (EKS) requires careful handling of deployment strategy,
traffic routing, and persistent storage so that existing connections and state are preserved.

1. Use Rolling Deployments

The most common approach is rolling updates.

Process:

Deploy new container version

Start new tasks/pods

Gradually stop old ones

Maintain minimum healthy capacity

In ECS this is configured via deployment configuration:

Example:

Minimum healthy percent: 100
Maximum percent: 200

This ensures new tasks start before old ones stop, preventing downtime.

2. Use Load Balancer Health Checks

Use Elastic Load Balancing (usually ALB) to route traffic only to healthy containers.

Upgrade flow:

User → ALB → New healthy containers

Old containers are removed from the load balancer only after new ones pass health checks.

3. Graceful Shutdown for Stateful Apps

For stateful applications:

Enable connection draining

Allow running requests to complete

Save state before termination

Example for Kubernetes:

terminationGracePeriodSeconds: 60

This allows the container to finish processing before shutdown.

4. Persistent Storage for State

Stateful workloads must use persistent storage like:

Amazon Elastic Block Store

Amazon Elastic File System

This ensures application state is not lost when pods/tasks restart.

5. Use Readiness and Liveness Probes (EKS)

In Kubernetes, configure probes:

Readiness probe → ensures pod receives traffic only when ready

Liveness probe → restarts unhealthy pods

Example:

readinessProbe:
  httpGet:
    path: /health
6. Use Blue-Green Deployment (Safer Approach)

For critical upgrades, use blue-green deployments.

Process:

Blue (current version) → serving traffic
Green (new version) → deployed separately
Switch traffic after validation

In ECS this can be implemented using:

AWS CodeDeploy

Benefits:

Instant rollback

No production impact during testing.

7. Handle Database Schema Changes

For stateful systems with database changes:

Best practice:

Apply backward-compatible schema

Deploy new application version

Remove old schema later

This prevents application breakage.

8. Auto Scaling Safety

Maintain capacity during deployment using:

Amazon EC2 Auto Scaling

Kubernetes Horizontal Pod Autoscaler

This ensures enough instances during rollout.

✅ Short Interview Answer (3–4 lines)
For zero-downtime upgrades on ECS or EKS, I use rolling deployments behind an ALB so new containers become healthy before old ones terminate. Stateful 
data is stored in persistent storage like EBS or EFS, and readiness probes ensure traffic only 
reaches healthy pods. For critical releases, I implement blue-green deployments using CodeDeploy, which allows safe testing and instant rollback.

-------------------------------------------------------------------------------------

Q.52) How can you ensure S3 data protection against accidental deletion and overwrites?
-> To protect data in Amazon S3 from accidental deletion or overwrites, AWS provides multiple
mechanisms that help ensure data durability, recovery, and compliance.

1. Enable S3 Versioning

The most important feature is S3 Versioning.

How it works:

Every object update creates a new version

Old versions are retained

Deleted objects can be recovered

Example:

file.txt (v1)
file.txt (v2)
file.txt (v3)

If someone deletes the file, you can restore a previous version.

2. Enable S3 Object Lock

Use Amazon S3 Object Lock to prevent objects from being deleted or modified.

Modes available:

Governance Mode

Only privileged users can override deletion

Compliance Mode

No user (even root) can delete objects before retention expires

Useful for:

Compliance

Audit requirements

3. Enable MFA Delete

Use MFA Delete with S3 versioning.

This requires multi-factor authentication to:

Delete object versions

Disable versioning

This prevents accidental or unauthorized deletions.

4. Use Bucket Policies and IAM Controls

Use strict policies via AWS Identity and Access Management.

Examples:

Deny s3:DeleteObject

Allow only specific roles to delete objects

Restrict access using conditions

Example policy logic:

Deny deletion unless user is admin role
5. Cross-Region Replication

Enable Amazon S3 Cross-Region Replication.

Benefits:

Replicates objects to another AWS region

Protects against regional failures

Creates backup copies

6. Use Lifecycle Policies

Use S3 lifecycle rules to move older versions to cheaper storage.

Example:

Move old versions to Amazon S3 Glacier

This keeps recovery copies while reducing cost.

7. Enable Monitoring and Alerts

Track deletion events using:

AWS CloudTrail

Amazon CloudWatch

You can alert when:

Object deletion occurs

Versioning is disabled

✅ Short Interview Answer (3–4 lines)
To protect S3 data from accidental deletion or overwrites, I enable S3 versioning so previous object versions can be restored. I also use S3 Object 
Lock and MFA Delete for stronger protection and implement strict IAM policies to restrict deletion permissions. For additional resilience, I configure 
cross-region replication and monitoring using CloudTrail and CloudWatch.

----------------------------------------------------------------------------------------------

Q.53) What's your approach to cost optimisation in an environment with EC2, RDS, and Lambda?
-> My approach to cost optimization in an AWS environment with EC2, RDS, and Lambda focuses on right
sizing resources, using approproate pricing models, monitoring usage, and elimib=nating unused resources.

1. Optimize EC2 Costs

For Amazon EC2, the main strategies are right-sizing and using cheaper purchasing options.

Right-Sizing Instances

Analyze CPU, memory, and network utilization using Amazon CloudWatch.

Actions:

Downsize overprovisioned instances

Move to newer generation instance types

Use Spot and Reserved Instances

Use Spot Instances for fault-tolerant workloads

Use Reserved Instances or Savings Plans for steady workloads

Example:

Baseline workload → Reserved Instances
Batch workload → Spot Instances
Auto Scaling

Use Amazon EC2 Auto Scaling to scale instances based on demand so you don’t pay for idle capacity.

2. Optimize RDS Costs

For Amazon Relational Database Service:

Right-Size DB Instances

Monitor:

CPU utilization

Memory

IOPS

Resize instances if they are underutilized.

Reserved Instances

Purchase RDS Reserved Instances for long-term workloads to reduce cost significantly.

Storage Optimization

Use gp3 storage instead of gp2

Enable storage autoscaling

Remove unused snapshots

Stop Non-Production Databases

Automatically stop dev/test databases during non-working hours.

3. Optimize Lambda Costs

For AWS Lambda, costs depend on execution time and memory allocation.

Right-Size Memory

Tune Lambda memory allocation because CPU scales with memory.

Use monitoring from Amazon CloudWatch to determine optimal values.

Reduce Execution Time

Optimize:

Code performance

Cold start time

External API calls

Use Provisioned Concurrency Carefully

Only enable when required because it increases cost.

4. Identify Idle Resources

Use tools like:

AWS Cost Explorer

AWS Trusted Advisor

Check for:

Idle EC2 instances

Unattached EBS volumes

Unused load balancers

Old snapshots

5. Use Storage Lifecycle Policies

For logs and backups stored in Amazon S3, configure lifecycle policies to move older data to cheaper tiers like Amazon S3 Glacier.

6. Implement Cost Monitoring and Budgets

Set up budgets using AWS Budgets to get alerts when spending exceeds thresholds.

This helps detect unexpected cost spikes early.

✅ Short Interview Answer (3–4 lines)
For cost optimization, I start with right-sizing EC2 and RDS instances based on CloudWatch metrics and use Savings Plans or Reserved 
Instances for steady workloads. I use Spot Instances and Auto Scaling for variable workloads and optimize Lambda by tuning memory and execution time.
I also monitor costs using Cost Explorer, Trusted Advisor, and AWS Budgets to identify idle or underutilized resources.

------------------------------------------------------------------------------------------

Q.54) How do you use AWS Config and CloudTrail for post-incident investigations and audit trails?
-> For post-incident investigations and audit trails. AWS Config and AWS CloudTrail work together 
to provide visibility into configuration changes and user/API activity across AWS resources.

1. Role of AWS CloudTrail (Who Did What)

CloudTrail records all API calls and user actions performed in the AWS account.

During an incident investigation, it helps answer:

Who performed the action?

When was it done?

What API call was executed?

From which IP address?

Example investigation steps:

Search CloudTrail logs for events like:

DeleteBucket

TerminateInstances

ModifySecurityGroup

Identify:

IAM user/role

Source IP

Timestamp

Example event record:

EventName: DeleteSecurityGroup
UserIdentity: IAM role
SourceIPAddress: 192.x.x.x

Logs are usually stored in Amazon S3 for long-term auditing.

2. Role of AWS Config (What Changed)

While CloudTrail shows who made the change, AWS Config shows how the resource configuration changed over time.

AWS Config tracks:

Resource configuration history

Compliance state

Configuration timeline

Example use case:

If a security group was modified, AWS Config shows:

Before: Port 22 open to internal CIDR
After: Port 22 open to 0.0.0.0/0

This helps identify misconfigurations that caused the incident.

3. Investigation Workflow

Typical post-incident investigation process:

1. Incident detected
2. Check CloudTrail → identify user/API activity
3. Use AWS Config → review resource configuration timeline
4. Determine root cause
5. Restore previous configuration

This combination provides a complete forensic trail.

4. Compliance and Audit Reporting

Both services support compliance audits.

Benefits:

Track configuration drift

Verify policy compliance

Maintain audit logs for security reviews

You can also integrate alerts with Amazon CloudWatch to detect suspicious changes in real time.

5. Long-Term Log Retention

Store logs securely in S3 with versioning and lifecycle policies to retain audit records for months or years depending on compliance requirements.

✅ Short Interview Answer (3–4 lines)
For post-incident investigations, I use AWS CloudTrail to identify who performed an action and which API calls were executed, including timestamps and source IPs. 
Then I use AWS Config to analyze the configuration history of affected resources and identify what changed over time. Together they provide a complete audit trail for root cause analysis and compliance reporting.

---------------------------------------------------------------------------------------

Q.55) A pod keeps restarting with CrashLoopBackOff. How would you troubleshoot this issue?

-> - Run kubectl describe pod to review events and diagnose the issue.
   - Use kubectl logs --previous to inspect crash logs.
   - check the container's liveness probes and image configurations for potential errors.
   - validate environment variables and secrets to ensure they are correctly set.

------------------------------------------------------------------------

Q.56) After deploying a new version, users report failures. How would you respond? (Rolling Update Gone Wrong)
-> - Execute kubectl rollout undo deployment to revert to the previous stable version.
   - Review the deployment's status with kubectl rollout status to monitor the update process.
   - Confirm that readiness and liveness probes are configured correctly to prevent faulty pods from joining the load balancer.

---------------------------------------------------------------

Q.57) your application requires persistent storage. How would you configure the pod specification? (App needs persistent storage)
-> - create a persistentVolume (PV) and persistentVolumeClaim (PVC) for storage management.
   - Attach the PVC to the pod specification under the volumes ans volume mounts sections.
   - Choose the appropriate storage class (e.g., gp2 for AWS, azure file for Azure) based on workload requirements.

------------------------------------------------------------------

Q.58) How would you manage kubernetes deployments across dev, staging, and production environments? (Multi-Environment Deployments)
-> - Leverage Helm or kustomise to perameterize your manifestos for environment-specific configurations.
   - Maintain environment-specific values, such as values-dev.yaml, in version control.
   - use namespaces or separate cluster to isolate resources for each environment, ensuring proper access control.

---------------------------------------------------------------------

Q.59) your pod id unable to access external APIs. What steps would you take to investigate? (Pod can't Reach External Services)
-> - verify the network policies in place to ensure no restrictions are blocking egress traffic.
   - Test DNS resolution inside the pod using tools like nslookup or dig to ensure proper connectivity.
   - confirm the node's egress and firewall rules to rule out any external network issues.
   - check the service account and proxy settings to ensure the pod has the correct permissions and network configuration.

--------------------------------------------------------------------------

Q.60) What's the difference between build and release pipelines in Azure DevOps?
-> In Azure DevOps, Build pipelines and Release pipelines are two stages of the CI/CD process.
They serve different purposes in the software delivery lifecycle.

1. Build Pipeline (Continuous Integration – CI)

A Build pipeline is responsible for compiling the code and creating deployable artifacts.

Key Functions:

Pulls source code from repositories (Git, etc.)

Compiles the application

Runs unit tests and static code analysis

Packages the application (JAR, Docker image, ZIP, etc.)

Produces build artifacts for deployment

Example:
Developer pushes code → Build pipeline runs → Code compiled → Tests executed → Artifact generated.

Output:
Artifacts like:

Docker images

JAR/WAR files

Binary packages

2. Release Pipeline (Continuous Delivery/Deployment – CD)

A Release pipeline takes the artifacts from the build pipeline and deploys them to environments.

Key Functions:

Deploys build artifacts to environments (Dev, QA, Staging, Prod)

Manages environment approvals and gates

Runs deployment scripts or configuration tasks

Supports rollback if deployment fails

Example:
Artifact from build → Deploy to Dev → Approval → Deploy to QA → Deploy to Production.

Output:
Running application in target environments.

----------------------------------------------------------------------------

Q.61) How do you handle rollback in your deployment pipeline?
-> 1) we can do tollback maunally using the kubernetes (kubectl rollout undo deploymenmt deployent_name).
   2) we will follow the deploymemnt strateges like blue-green deployment, canary deployment and rolling update.
   - Blue-Green -> Switch traffic back to previous successful version if failuer occure.
   - Canary Deployment -> deploy to small % of user, monitor, then proceed or rollback.
   - Rolling-Update -> if helth checks fails, pipeline auto-stoped and revert.

----------------------------------------------------------------------------

Q.62) How do you secure secrets and credentials in your pipeline?
-> In a DevOps pipeline, securing secrets and credentials is done using centralized secret management, 
least-privilege access, and avoiding hardcoded credentials in code or scripts.
Key Practices

1. Use a Secret Management System
Store secrets in secure services like AWS Secrets Manager, HashiCorp Vault, or Azure Key Vault instead of storing them in code repositories or configuration files.

2. Use Environment Variables / Secret Variables
Inject secrets into pipelines as encrypted environment variables so they are masked in logs and only available during runtime.

3. Use IAM Roles or Identity-Based Access
Avoid static credentials and use role-based access via AWS Identity and Access Management (IAM roles, service accounts, workload identities) to grant temporary permissions.

4. Restrict Access with Least Privilege
Limit access to secrets using RBAC and ensure only required pipelines or services can retrieve them.

5. Secret Rotation and Expiry
Enable automated credential rotation and enforce periodic secret updates.

6. Prevent Secret Exposure

Mask secrets in logs

Avoid printing them in scripts

Use encrypted storage for pipeline artifacts.

Short Interview Answer (2–3 lines)

In DevOps pipelines, I secure secrets using centralized secret managers like AWS Secrets Manager or HashiCorp Vault and inject them at runtime as encrypted environment variables. I also use IAM roles via AWS Identity and Access Management instead of static credentials, enforce least-privilege access, and ensure secrets are masked in logs with regular rotation.

-------------------------------------------------------------------------------------------

Q.63) what's the difference between ENTRYPOINT and CMD in Docker?
-> In Docker, both ENTRYPOINT and CCMD are instructions used in a Dockerfile to define what command 
runs when a container starts, but they behave differently.

1. ENTRYPOINT

ENTRYPOINT defines the main executable that will always run when the container starts.

It cannot easily be overridden by command-line arguments.

Any arguments passed with docker run are appended to the ENTRYPOINT command.

Example:

ENTRYPOINT ["python", "app.py"]

Run command:

docker run myimage --port 8080

Actual command executed:

python app.py --port 8080
2. CMD

CMD provides default arguments for the container, or a default command if ENTRYPOINT is not defined.

It can be overridden when running the container.

Example:

CMD ["python", "app.py"]

Run command:

docker run myimage python test.py

Actual command executed:

python test.py
When Both Are Used Together

Typically, ENTRYPOINT defines the main command and CMD provides default parameters.

Example:

ENTRYPOINT ["python", "app.py"]
CMD ["--port", "8080"]

Run command:

docker run myimage

Executed:

python app.py --port 8080

Short Interview Answer (2–3 lines)

In Docker, ENTRYPOINT defines the main command that always runs when the container starts, 
while CMD provides default arguments or a fallback command. CMD can be overridden at runtime,
whereas ENTRYPOINT is usually fixed and any runtime arguments are appended to it.

-----------------------------------------------------------------------------

Q.64) what's steps do you follow when a container crashes repeatedly?
-> When a container crashes repeatedly in Docker or orchestration platforms like Kubernetes, I 
follow a structured troubleshooting approach to identify the root cause.

1. Check Container Status

First verify the container state and restart count.

docker ps -a
docker inspect <container_id>

In Kubernetes:

kubectl get pods
kubectl describe pod <pod-name>
2. Check Container Logs

Logs usually reveal the immediate reason for the crash.

docker logs <container_id>

In Kubernetes:

kubectl logs <pod-name>
3. Identify Crash Reason

Common causes include:

Application errors

Missing environment variables

Incorrect configuration

Dependency or database connection failures

Port conflicts

4. Validate Image and Configuration

Check:

Docker image version

Environment variables

Secrets/config maps

Startup commands (ENTRYPOINT/CMD)

5. Check Resource Limits

Sometimes containers crash due to CPU or memory limits.
In Kubernetes check if it is OOMKilled.

Example:

kubectl describe pod <pod-name>
6. Test the Container Locally

Run the container interactively to reproduce the issue.

docker run -it <image> /bin/bash
7. Check Dependencies

Verify connectivity to external services:

Database

API endpoints

Message queues

Storage services

8. Review Health Checks

Improper liveness/readiness probes in Kubernetes can cause repeated restarts.

9. Check Recent Changes

Review:

Recent deployments

Image updates

Config or secret changes in CI/CD pipeline

Short Interview Answer (3–4 lines)

When a container crashes repeatedly in Docker or Kubernetes, I first check container status and logs to identify the error. Then I verify configuration, environment variables, 
and resource limits to rule out issues like misconfiguration or OOM kills. If needed, I run the container interactively to reproduce the issue and check dependencies or recent deployment changes.

---------------------------------------------------------------------------------------------

Q.65) How do you perform a Zero-downtime deployment in Kubernetes?
-> Zero-downtime deployment in kubernetes ensures that application updates happen without interruptiong
user traffic. This is typically achieved using rolling updates, health checks, and proper traffic routing.

1. Use Rolling Updates

Configure the Deployment strategy to update pods gradually.

Example:

strategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 0
    maxSurge: 1

maxUnavailable: 0 → ensures existing pods stay available

maxSurge: 1 → creates one extra pod during update

This ensures new pods start before old pods terminate.

2. Configure Readiness Probes

Use readiness probes so traffic is sent only to healthy pods.

Example:

readinessProbe:
  httpGet:
    path: /health
    port: 8080

Until the probe passes, the pod will not receive traffic from the service.

3. Use Liveness Probes

Liveness probes help automatically restart unhealthy containers to maintain availability.

4. Use Multiple Replicas

Deploy multiple pod replicas so traffic continues even while pods are being replaced.

Example:

replicas: 3
5. Graceful Shutdown

Use terminationGracePeriodSeconds so old pods finish ongoing requests before stopping.

6. Use Load Balancing via Services

Kubernetes Service objects automatically route traffic only to healthy pods during updates.

7. Advanced Deployment Strategies (Optional)

For critical production workloads:

Blue-Green Deployment

Canary Deployment

Service mesh tools like Istio for traffic splitting.

Short Interview Answer (2–3 lines)

In Kubernetes, zero-downtime deployments are achieved using rolling updates with multiple replicas, ensuring new pods become ready before old ones terminate. 
I configure readiness probes, liveness probes, and proper surge/unavailable settings, and use services or canary/blue-green strategies to safely shift traffic during deployments. 🚀

---------------------------------------------------------------------------------

Q.66) What is a readiness probe vs a liveness probe?
-> in Kubernetes, readiness probes and liveness probes are health checks used to manage container availability
and reliability.

1. Readiness Probe

A readiness probe checks whether a container is ready to receive traffic.

If the readiness probe fails, the pod is removed from the service endpoint list.

Traffic will not be routed to that pod until it becomes ready again.

The container keeps running, but it won’t serve requests.

Use Case:
If the application is still starting up, loading configuration, or waiting for a database connection.

Example:

readinessProbe:
  httpGet:
    path: /health
    port: 8080
2. Liveness Probe

A liveness probe checks whether the application inside the container is still running properly.

If the liveness probe fails, Kubernetes restarts the container automatically.

Use Case:
If the application is stuck, deadlocked, or unresponsive.

Example:

livenessProbe:
  httpGet:
    path: /health
    port: 8080
Key Differences
Feature	Readiness Probe	Liveness Probe
Purpose	Checks if pod can receive traffic	Checks if container is alive
Failure Action	Pod removed from service	Container restarted
Container Status	Keeps running	Restarted
Short Interview Answer (2–3 lines)

In Kubernetes, a readiness probe checks if a pod is ready to receive traffic and removes it from service endpoints if it fails.
A liveness probe checks if the application is still running correctly and restarts the container if it becomes unresponsive.

---------------------------------------------------------------------------------

Q.67) how do you manage application configuration in EKS?
-> Managing application configuration in Amazon Elastic Kubernetes Service (EKS) typically involves 
separating configuration from container images and using kubernetes-native resources.

1. Use ConfigMaps

ConfigMaps store non-sensitive configuration such as environment variables, application settings, and configuration files.

Example:

apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  ENV: production
  LOG_LEVEL: INFO

ConfigMaps can be mounted as:

Environment variables

Files inside containers

2. Use Secrets for Sensitive Data

Sensitive data like passwords, tokens, or API keys are stored in Secrets.

Example:

apiVersion: v1
kind: Secret
metadata:
  name: db-secret
type: Opaque
data:
  password: base64encodedvalue

For better security, secrets can be integrated with AWS Secrets Manager or AWS Systems Manager Parameter Store.

3. Use External Secret Integrations

Use tools like:

AWS Secrets Manager

HashiCorp Vault

These integrate with Kubernetes using controllers to automatically sync secrets into the cluster.

4. Use Environment-Specific Configuration

For different environments (Dev, QA, Prod), use tools like:

Helm values files

Kustomize overlays

This allows managing configuration differences cleanly.

5. Use IAM Roles for Service Accounts (IRSA)

Instead of storing AWS credentials, assign permissions using AWS Identity and Access Management roles mapped to Kubernetes service accounts.

Short Interview Answer (2–3 lines)

In Amazon Elastic Kubernetes Service, application configuration is managed using ConfigMaps for non-sensitive data and Secrets for sensitive data. 
For better security and centralized management, I integrate with AWS Secrets Manager or Parameter Store and use Helm/Kustomize for environment-specific configurations.

-----------------------------------------------------------------------

Q.68) what monitoring tools have you used for kubernetes?
-> For monitoring Kubernetes, I have used a combination of metrics, logging, and visualization tools 
to monitor cluster health, application performance, and infrastructure resources.

1. Prometheus

Prometheus is widely used for Kubernetes metrics collection.

Scrapes metrics from nodes, pods, and services

Stores time-series data

Used for alerting with Alertmanager

2. Grafana

Grafana is used for visualizing metrics and building dashboards.

Displays CPU, memory, pod usage, and latency metrics

Integrates with Prometheus as a data source.

3. ELK / EFK Stack

For log monitoring and analysis:

Elasticsearch – log storage and indexing

Fluentd – log collection

Kibana – log visualization

4. Cloud Monitoring Tools

For managed Kubernetes like Amazon Elastic Kubernetes Service, I have used:

Amazon CloudWatch for metrics and logs

Container Insights for pod and node monitoring.

5. Kubernetes Native Monitoring

kube-state-metrics for cluster state metrics

metrics-server for CPU/memory metrics used by HPA.

Short Interview Answer (2–3 lines)

For Kubernetes monitoring, I typically use Prometheus for metrics collection and Grafana for dashboards and visualization. 
For logging, I use the EFK stack with Elasticsearch and Kibana, and in AWS environments I also leverage Amazon CloudWatch for centralized monitoring and alerts.

---------------------------------------------------------------------------------

Q.69) how do you centralise logs in a micro services environment?
-> in a microservices environment, centralized logging helps collect logs from multiple services and
analyze them in one place for troubleshooting and monitoring.

1. Use a Log Aggregation System

Deploy a centralized logging stack such as:

Elasticsearch for storing and indexing logs

Logstash or Fluentd for log processing

Kibana for visualization and searching logs

This combination is commonly called the ELK or EFK stack.

2. Use Log Collectors / Agents

Run log collectors on each node or container to collect logs and forward them to the centralized system.

Common agents:

Fluentd

Filebeat

Fluent Bit

In Kubernetes, these are usually deployed as DaemonSets.

3. Use Structured Logging

Applications should produce JSON or structured logs with fields like:

service name

request ID

timestamp

log level

This makes searching and correlation easier.

4. Correlate Logs with Request IDs

Add a trace ID or correlation ID so logs from multiple microservices can be tracked for a single request.

5. Use Cloud Logging (Optional)

For cloud platforms such as Amazon Web Services, logs can also be centralized using Amazon CloudWatch.

Short Interview Answer (2–3 lines)

In a microservices environment, I centralize logs using a logging stack like Elasticsearch, Fluentd, and Kibana. Log collectors run on each node or container to ship logs to a central system, 
and I use structured logging with correlation IDs to trace requests across services.

---------------------------------------------------------------------------

Q.70) what your approach to setting up alerts in AWS Monitor?
-> My approach to setting up alerts in Amazon CloudWatch focuses on monitoring critical metrics, defining
meaningful thresholds, and ensuring actionable notifications.

1. Identify Critical Metrics

First, I determine which metrics are important for the service, such as:

CPU utilization for EC2

Memory usage

Application latency

Error rates

Disk and network metrics

These metrics come from services like Amazon EC2, Amazon RDS, or container workloads.

2. Create CloudWatch Alarms

I configure alarms in Amazon CloudWatch based on thresholds or anomaly detection.

Example:

CPU > 80% for 5 minutes

Error rate > 5%

Disk space < 20%

3. Configure Notifications

Connect alarms to Amazon Simple Notification Service (SNS) to send alerts through:

Email

Slack/Webhook

SMS

4. Implement Multi-Level Alerting

Define severity levels:

Warning – early indication of a problem

Critical – immediate action required

5. Reduce Alert Noise

Use:

Proper thresholds

Aggregated metrics

Alert suppression or deduplication

6. Automate Remediation (Optional)

For common issues, configure alarms to trigger automated actions like:

Auto Scaling

Restart instances

Run Lambda functions.

Short Interview Answer (2–3 lines)

My approach is to monitor key infrastructure and application metrics in Amazon CloudWatch, then create alarms with meaningful thresholds. 
I integrate alerts with Amazon Simple Notification Service to notify teams via email or Slack and avoid alert fatigue by setting proper thresholds and severity levels.

------------------------------------------------------------------------------------

Q.71) How do you handle secrets management across environments?
-> Handling secrets across environments (Dev, QA, Prod) requires centralized storage, secure access control,
and environment isolation.

1. Use a Centralized Secret Manager

Store all secrets in a secure service like:

AWS Secrets Manager

HashiCorp Vault

AWS Systems Manager Parameter Store

This avoids hardcoding credentials in code, repositories, or configuration files.

2. Separate Secrets by Environment

Maintain different secrets for each environment such as:

dev/db-password

qa/db-password

prod/db-password

Access policies ensure that only the correct environment can retrieve its secrets.

3. Use Role-Based Access

Grant access using AWS Identity and Access Management roles or service identities instead of static credentials.
For example:

Dev environment → limited permissions

Production → restricted to production services only.

4. Inject Secrets at Runtime

Secrets are injected during application startup through:

Environment variables

Kubernetes Secrets

Mounted files in containers.

5. Enable Encryption and Rotation

Encrypt secrets using KMS

Enable automatic secret rotation to improve security.

6. Avoid Secret Exposure

Mask secrets in logs and CI/CD pipelines

Restrict viewing permissions

Audit access to secrets.

Short Interview Answer (2–3 lines)

I manage secrets using centralized tools like AWS Secrets Manager or HashiCorp Vault, with separate secrets for Dev, QA, and Prod environments. 
Access is controlled through AWS Identity and Access Management roles, and secrets are injected at runtime with encryption and automatic rotation enabled.

------------------------------------------------------------------------------------

Q.72) What is the parameter group in Amazon RDS, and what is its purpose?
-> In Amazon RDS, a parameter Group is a collection of database configuration settings that
control the behavior of the database engine.

Purpose of a Parameter Group

Parameter groups allow you to customize database engine settings without modifying the database software itself. These parameters influence how the database performs operations.

Examples of parameters:

max_connections

query_cache_size

log_statement

work_mem

innodb_buffer_pool_size

Types of Parameter Groups

Default Parameter Group

Provided by Amazon RDS

Cannot be modified.

Custom Parameter Group

Created by users.

Allows modification of database configuration settings.

How It Works

A parameter group is attached to an RDS instance.

When parameters are changed:

Some changes apply immediately (dynamic).

Some require a database reboot (static).

Why It Is Important

Parameter groups help with:

Database performance tuning

Controlling logging behavior

Managing connection limits

Standardizing configurations across multiple RDS instances.

Short Interview Answer (2–3 lines)

In Amazon RDS, a Parameter Group is a set of configuration settings that control the behavior of the database engine, 
such as connection limits or memory allocation. It allows users to customize and tune database performance without modifying the database software itself.

------------------------------------------------------------------------------------

Q.73) How do I write a CloudFormation template to deploy both an EC2 instance and an Application Load Balancer (ALB).
-> To deploy an EC2 instance and an Application Load Balancer using AWS CloudFormation, you define 
resources such as a Security Group, Target Group, ALB, Listener, and EC2 instance in a YAML or JSON template.

Below is a simple YAML example.

Example CloudFormation Template
AWSTemplateFormatVersion: "2010-09-09"
Description: Deploy EC2 with Application Load Balancer

Resources:

  MySecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Allow HTTP access
      VpcId: vpc-xxxxxxxx
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 80
          ToPort: 80
          CidrIp: 0.0.0.0/0

  MyEC2Instance:
    Type: AWS::EC2::Instance
    Properties:
      InstanceType: t2.micro
      ImageId: ami-xxxxxxxx
      SecurityGroupIds:
        - !Ref MySecurityGroup
      SubnetId: subnet-xxxxxxxx

  MyTargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Port: 80
      Protocol: HTTP
      VpcId: vpc-xxxxxxxx
      TargetType: instance
      Targets:
        - Id: !Ref MyEC2Instance
          Port: 80

  MyALB:
    Type: AWS::ElasticLoadBalancingV2::LoadBalancer
    Properties:
      Name: my-alb
      Subnets:
        - subnet-xxxx1
        - subnet-xxxx2
      SecurityGroups:
        - !Ref MySecurityGroup
      Scheme: internet-facing
      Type: application

  MyListener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      LoadBalancerArn: !Ref MyALB
      Port: 80
      Protocol: HTTP
      DefaultActions:
        - Type: forward
          TargetGroupArn: !Ref MyTargetGroup
Components Explained

Amazon EC2 → Creates the compute instance.

Application Load Balancer → Distributes incoming traffic.

Target Group → Registers EC2 instance as backend target.

Listener → Accepts HTTP requests and forwards them to the target group.

Security Group → Allows inbound HTTP traffic.

Deployment Command
aws cloudformation create-stack \
  --stack-name ec2-alb-stack \
  --template-body file://template.yaml
Short Interview Answer (2–3 lines)

To deploy EC2 with an Application Load Balancer using AWS CloudFormation, I define resources such as an EC2 instance, ALB, target group, listener, 
and security groups in the template. The ALB listener forwards incoming traffic to the target group where the EC2 instance is registered as the backend target.

-----------------------------------------------------------------------------------

Q.74) What are ECS (Elastic Container Service) and ECR (Elastic Container Registry) in the context of containerisation?
-> 1. ECS (Elastic Container Service)

Amazon Elastic Container Service (ECS) is a container orchestration service used to run and manage Docker containers on AWS.

Key features:

Runs containerized applications at scale

Manages container scheduling and lifecycle

Supports EC2 launch type or Fargate (serverless containers)

Integrates with load balancers, IAM, and auto scaling.

2. ECR (Elastic Container Registry)

Amazon Elastic Container Registry (ECR) is a managed Docker image repository where container images are stored.

Key features:

Securely store Docker images

Integrates with ECS and EKS

Supports image versioning and lifecycle policies

Provides vulnerability scanning.

How ECS and ECR Work Together

Build a Docker image locally.

Push the image to Amazon Elastic Container Registry.

Create a task definition in Amazon Elastic Container Service referencing that image.

ECS pulls the image from ECR and runs containers.

Short Interview Answer (2–3 lines)

Amazon Elastic Container Service (ECS) is a container orchestration service used to run and manage containerized applications on AWS. 
Amazon Elastic Container Registry (ECR) is a managed container image repository where Docker images are stored. ECS pulls container images from ECR to deploy and run containers.

------------------------------------------------------------------------------

Q.75) How do I create Docker Images and push them to Amazon ECR?
-> To create a Docker image and push it to Amazon Elastic Container Registry (ECR), follow these below 
steps.

1. Create a Dockerfile

First create a Dockerfile for your application.

Example:

FROM nginx:latest
COPY index.html /usr/share/nginx/html
2. Build the Docker Image

Build the image using Docker.

docker build -t myapp .

Verify the image:

docker images
3. Create an ECR Repository

Create a repository in Amazon Elastic Container Registry.

Example using CLI:

aws ecr create-repository --repository-name myapp
4. Authenticate Docker to ECR

Login to ECR using AWS Command Line Interface.

aws ecr get-login-password --region us-east-1 \
| docker login --username AWS --password-stdin <account-id>.dkr.ecr.us-east-1.amazonaws.com
5. Tag the Docker Image

Tag the image with the ECR repository URI.

docker tag myapp:latest <account-id>.dkr.ecr.us-east-1.amazonaws.com/myapp:latest
6. Push the Image to ECR

Push the image to the repository.

docker push <account-id>.dkr.ecr.us-east-1.amazonaws.com/myapp:latest
7. Verify the Image

Go to Amazon Elastic Container Registry console and confirm the image is uploaded.

✅ Workflow Summary

Build Docker image

Create ECR repository

Authenticate Docker with AWS

Tag image with ECR URI

Push image to ECR

Short Interview Answer (2–3 lines)

To push images to Amazon Elastic Container Registry, I first build the Docker image using Docker, then create an ECR repository and authenticate using the AWS Command Line Interface. 
After tagging the image with the ECR repository URI, I push it using docker push.

-------------------------------------------------------------------------------

Q.76) In a multi-account setup using AWS CloudFormation, how can I create a single IAM role across all accounts?
-> In a multi-account AWS environment, you can create the same IAM role across all accounts using 
stack deployment from a central account.

Common Approach: CloudFormation StackSets

Use AWS CloudFormation StackSets to deploy resources across multiple accounts and regions.

AWS CloudFormation StackSets allow you to:

Deploy a single template to multiple accounts

Maintain consistent configuration across accounts

Automatically apply updates when the template changes.

Steps to Create a Single IAM Role Across Accounts

1. Create a CloudFormation Template

Example template defining an IAM role:

AWSTemplateFormatVersion: '2010-09-09'
Resources:
  CrossAccountRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: OrganizationAccessRole
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              AWS: "*"
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/ReadOnlyAccess

2. Use StackSets
Deploy this template using AWS CloudFormation StackSets.

You specify:

Target accounts

Target regions

Deployment permissions.

3. Use AWS Organizations (Recommended)
If using AWS Organizations, you can target Organizational Units (OUs) instead of listing accounts manually.

This allows:

Automatic deployment to new accounts

Centralized governance.

4. Enable Service-Managed Permissions
Using AWS Organizations with StackSets enables automatic role creation in all accounts.

Benefits

Consistent IAM role configuration

Centralized management

Automatic deployment to new accounts

Easier governance and auditing.

Short Interview Answer (2–3 lines)

In a multi-account environment, I use AWS CloudFormation StackSets to deploy an IAM role template across multiple accounts. With AWS Organizations, StackSets can target organizational units so the role is automatically created and maintained consistently across all accounts.

---------------------------------------------------------------------------------

Q.77) If I have a 450MB Docker image, what are the best practices to optimise and reduce its size?
-> if a Docker image is around 450MB, there are several best practices to optimize and reduce the image 
size for faster builds, quicker deployments, and lower storage/network usage.

1. Use a Smaller Base Image

Choose lightweight base images instead of large ones.

Examples:

Alpine Linux based images

node:alpine, python:alpine

This alone can reduce image size significantly.

2. Use Multi-Stage Builds

Use multi-stage builds to keep build dependencies out of the final image.

Example:

FROM node:18 AS builder
WORKDIR /app
COPY . .
RUN npm install && npm run build

FROM node:18-alpine
WORKDIR /app
COPY --from=builder /app/dist /app
CMD ["node","app.js"]

Only the final artifacts are copied to the production image.

3. Remove Unnecessary Packages

Avoid installing tools that are not needed at runtime.

Example:

RUN apt-get update && apt-get install -y build-essential \
 && rm -rf /var/lib/apt/lists/*
4. Combine RUN Commands

Combining commands reduces layers in the image.

Example:

RUN apt-get update && apt-get install -y curl git
5. Use .dockerignore

Exclude unnecessary files such as:

.git

logs

local configs

node_modules (if rebuilding inside container)

Example .dockerignore:

.git
*.log
node_modules
tests
6. Clean Package Cache

Remove temporary files and caches after installation.

Example:

RUN apt-get clean && rm -rf /var/lib/apt/lists/*
7. Use Minimal Runtime Images

Use runtime-only images like:

distroless

Alpine

slim versions

Example:
python:3.11-slim

8. Scan and Optimize Layers

Use tools like:

Docker Scout

Dive

These help identify large layers and unnecessary files.

Short Interview Answer (2–3 lines)

To reduce a Docker image size, I use lightweight base images like Alpine Linux, implement multi-stage builds, and remove unnecessary packages or caches. I also use .dockerignore to exclude unused files and combine RUN commands to reduce image layers.

-----------------------------------------------------------------------------

Q.78) What is the difference between Docker and Kubernetes in container orchestration?
-> Docker vs Kubernetes in Container Orchestration

Docker and Kubernetes both work with containers, but they serve different purposes.

1. Docker

Docker is a containerization platform used to build, package, and run applications inside containers.

Key functions:

Build container images

Run containers on a host

Manage container lifecycle on a single machine

Package applications with dependencies.

Example:

docker build -t myapp .
docker run myapp
2. Kubernetes

Kubernetes is a container orchestration platform used to manage containers at scale across multiple servers.

Key functions:

Automates container deployment

Auto-scaling of applications

Load balancing

Self-healing (restarts failed containers)

Rolling updates and zero-downtime deployments.

How They Work Together

Typically:

Applications are packaged using Docker images.

Containers are deployed and managed by Kubernetes clusters.

Short Interview Answer (2–3 lines)

Docker is used to build and run containerized applications, while Kubernetes is used to orchestrate and manage those containers across multiple servers. Kubernetes provides capabilities like scaling, self-healing, and load balancing for containerized workloads.

------------------------------------------------------------------------

Q.79) What is a pod in Kubernetes, and how does it function?
-> A pod is the smallest deployable unit in Kubernetes and represents a running instance of one containers.

Key Characteristics of a Pod

1. Single or Multiple Containers
A pod usually contains one container, but it can also contain multiple containers that must run together and share resources.

2. Shared Network
All containers inside a pod share:

the same IP address

the same network namespace

the same port space

Containers communicate with each other using localhost.

3. Shared Storage
Containers in a pod can share storage volumes defined in the pod specification.

Example:

apiVersion: v1
kind: Pod
metadata:
  name: myapp
spec:
  containers:
  - name: nginx
    image: nginx

4. Ephemeral Nature
Pods are temporary. If a pod crashes or is deleted, Kubernetes usually creates a new one using controllers like Deployments or ReplicaSets.

Example Use Case

A pod could contain:

Main application container

Sidecar container for logging or monitoring.

Short Interview Answer (2–3 lines)

A Pod in Kubernetes is the smallest deployable unit that runs one or more containers. Containers inside a pod share the same network, IP address, and storage, allowing them to communicate and operate together as a single application unit.

-------------------------------------------------------------------

Q.80) what is the difference between Docker and Kubernetes in container orcjestration?
->
--------------------------------------------------------------------------------------

Q.81) How Is load balancing achieved within pods in Kubernetes?
-> In Kubernetes, load balancing is not done inside a single pod, but across multiple pods using Kubernetes
Sevices and other components.

1. Kubernetes Service (Primary Method)

A Service distributes incoming traffic across multiple pods that match a label selector.

Example:

apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  selector:
    app: myapp
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP

The service automatically load balances traffic among the pods.

2. kube-proxy

kube-proxy runs on each node and handles the actual traffic routing.
It uses techniques like:

iptables

IPVS

to distribute requests across pods.

3. External Load Balancer

For external traffic, Kubernetes integrates with cloud load balancers like Application Load Balancer or Network Load Balancer.

Traffic flow example:
Client → Load Balancer → Service → Pods

4. Ingress Controller

For HTTP/HTTPS routing, an Kubernetes Ingress controller can distribute traffic to services based on hostnames or paths.

Key Concept

Pods are replicated using Deployments.

Services distribute traffic across these pods.

Short Interview Answer (2–3 lines)

In Kubernetes, load balancing is achieved using Services, which distribute traffic across multiple pods using label selectors. Internally, kube-proxy routes traffic using iptables or IPVS, while external traffic can be balanced using cloud load balancers or Ingress controllers.

------------------------------------------------------------------------

Q.82) How can I fetch all EC2 instance across all AWS accounts and regions?
-> to fetch all EC2 instances across multiple AWS accounts and regions, you typically use cross-account
access with automation scripts.

1. Use AWS Organizations + Cross-Account IAM Role

In a multi-account setup with AWS Organizations, create a common IAM role in each account that allows the central account to assume it using AWS Identity and Access Management (IAM).

Example role:

OrganizationAccountAccessRole

Allow sts:AssumeRole.

2. Assume the Role in Each Account

From the central account, use AWS Security Token Service (STS) to assume the role in each target account.

Example:

aws sts assume-role \
  --role-arn arn:aws:iam::<account-id>:role/OrganizationAccountAccessRole \
  --role-session-name ec2-audit
3. Loop Through All AWS Regions

Use Amazon EC2 API to list instances for each region.

Command:

aws ec2 describe-instances --region us-east-1

First fetch all regions:

aws ec2 describe-regions --query "Regions[].RegionName" --output text
4. Automate With Script

Use a Bash or Python script to:

List all accounts

Assume role in each account

Loop through regions

Fetch EC2 instances.

Example logic:

for account in account_list
do
  assume-role
  for region in regions
  do
     aws ec2 describe-instances --region $region
  done
done
5. Alternative (Better for Large Organizations)

Use AWS Config or AWS Resource Explorer to query resources across accounts.

Best Practices

Use a central inventory account

Store results in S3 or DynamoDB

Schedule the script using Lambda or cron.

Short Interview Answer (2–3 lines)

To fetch all **Amazon EC2 instances across accounts and regions, I use AWS Organizations with a cross-account IAM role and assume it using AWS Security Token Service. Then I run a script that loops through all AWS regions and calls describe-instances to collect the data.

------------------------------------------------------------------------------------------

Q.83) How can I fetch all EC2 instances from all accounts within a specific AWS Organisational Unit (OU)?
-> To fetch all EC2 instances from all AWS accounts inside a specific Organizational Unit (OU), the 
typical approach is to use AWS Organizations + cross-account role access + a script (CLI/Python/Boto3).

Below is the standard architecture used in DevOps/SRE environments.

1️⃣ Prerequisites

Your management account must have access to AWS Organizations.

Each member account should have a cross-account IAM role (for example: OrganizationAccountAccessRole or a custom role).

The role must allow:

ec2:DescribeInstances

sts:AssumeRole

You will assume this role using AWS Security Token Service.

Method 1 — Python Script (Recommended)

Using Boto3.

Steps

Get account list from OU

Assume role in each account

Call Amazon EC2 API to fetch instances.

Example Python Script
import boto3

OU_ID = "ou-xxxx-yyyy"
ROLE_NAME = "OrganizationAccountAccessRole"

org_client = boto3.client('organizations')
sts_client = boto3.client('sts')

def get_accounts_from_ou(ou_id):
    accounts = []
    paginator = org_client.get_paginator('list_accounts_for_parent')
    for page in paginator.paginate(ParentId=ou_id):
        for acc in page['Accounts']:
            accounts.append(acc['Id'])
    return accounts

def assume_role(account_id):
    role_arn = f"arn:aws:iam::{account_id}:role/{ROLE_NAME}"
    response = sts_client.assume_role(
        RoleArn=role_arn,
        RoleSessionName="EC2InventorySession"
    )
    creds = response['Credentials']
    return boto3.client(
        'ec2',
        aws_access_key_id=creds['AccessKeyId'],
        aws_secret_access_key=creds['SecretAccessKey'],
        aws_session_token=creds['SessionToken']
    )

accounts = get_accounts_from_ou(OU_ID)

for account in accounts:
    ec2 = assume_role(account)
    response = ec2.describe_instances()
    print(f"\nAccount: {account}")

    for reservation in response['Reservations']:
        for instance in reservation['Instances']:
            print(instance['InstanceId'], instance['InstanceType'], instance['State']['Name'])
Method 2 — Using AWS CLI (Shell Script)

You can combine AWS CLI with STS.

Step 1 — Get accounts in OU
aws organizations list-accounts-for-parent --parent-id ou-xxxx-yyyy
Step 2 — Assume role and fetch instances

Example loop:

for account in $(aws organizations list-accounts-for-parent \
--parent-id ou-xxxx-yyyy \
--query 'Accounts[].Id' --output text)

do
  CREDS=$(aws sts assume-role \
    --role-arn arn:aws:iam::$account:role/OrganizationAccountAccessRole \
    --role-session-name ec2fetch)

  export AWS_ACCESS_KEY_ID=$(echo $CREDS | jq -r .Credentials.AccessKeyId)
  export AWS_SECRET_ACCESS_KEY=$(echo $CREDS | jq -r .Credentials.SecretAccessKey)
  export AWS_SESSION_TOKEN=$(echo $CREDS | jq -r .Credentials.SessionToken)

  echo "Account: $account"

  aws ec2 describe-instances \
    --query 'Reservations[].Instances[].InstanceId'
done
Method 3 — Enterprise Approach (Best Practice)

For large organizations (100+ accounts), use:

AWS Config

AWS Resource Explorer

AWS Systems Manager

AWS Athena inventory queries

These give centralized multi-account inventory without scripting.

🔹 SRE / DevOps Best Practice

Create a central inventory Lambda using:

AWS Lambda

Amazon EventBridge (scheduled daily)

Store results in Amazon S3 or Amazon DynamoDB.

This creates an organization-wide EC2 inventory dashboard.

✅ If you'd like, I can also show you a very powerful one-liner script used by DevOps engineers to fetch EC2 instances from 100+ AWS accounts in an OU in ~5 seconds. It’s very useful for SRE interviews and real environments.

----------------------------------------------------------------------------------------------

Q.84) if two VPCs in different AWS accounts have the same CIDR block, can VPC peering be established between them, and what are the alternatives?
-> No, VPC peering cannot be established if two VPCs have overlapping or identical CIDR blocks, even if they
are in different AWS accounts.
when using Amazon VPC Peering, AWS requires that the CIDR ranges of both VPCs must not overlap. if 
both VPCs use the same CIDR (for example 10.0.0.0/16), AWS will reject the peering request.

Alternatives for Overlapping CIDRs
1️⃣ PrivateLink (Most Common Solution)

Use AWS PrivateLink to expose a service from one VPC to another.

How it works:

Provider VPC exposes a service using Network Load Balancer

Consumer VPC connects using an interface endpoint

Advantages:

Works with overlapping CIDRs

Secure and scalable

Cross-account supported

Typical use case:

Accessing internal microservices across accounts

2️⃣ NAT + Transit Gateway

Use AWS Transit Gateway with NAT translation.

Architecture:

VPC A (10.0.0.0/16)
      |
   NAT Gateway
      |
Transit Gateway
      |
VPC B (10.0.0.0/16)

Traffic gets translated to another IP range before reaching the other VPC.

Use case:

Large enterprise networks

Multiple VPC connectivity

3️⃣ AWS VPC Lattice (Modern Service Networking)

Use Amazon VPC Lattice.

Features:

Connect services across multiple VPCs and accounts

Works even with overlapping CIDRs

Service-based connectivity instead of network-based

4️⃣ Proxy or Application Gateway

Deploy a proxy such as:

NGINX

HAProxy

Traffic flows through the proxy instead of direct network routing.

What Most Companies Do (Real-world SRE practice)

Usually they choose:

Scenario	Solution
Service-to-service access	PrivateLink
Multi-VPC enterprise network	Transit Gateway + NAT
Modern microservices	VPC Lattice

✅ Short interview answer (recommended):

VPC peering cannot be established if two VPCs have overlapping CIDR blocks because AWS routing requires unique IP ranges. In such cases, alternatives include using AWS PrivateLink to expose services via an NLB, implementing NAT with Transit Gateway for IP translation, or using VPC Lattice for service-level connectivity across VPCs and accounts.

-----------------------------------------------------------------------------------

Q.85) we usually see 2/2 status check on EC2 instances; what does it mean if we now see a 3/3 status check?
-> Normally an Amazon EC2 instance shows 2/2 status checks passed, which means both standard health checks
are successful.
Standard EC2 Status Checks (2/2)

System Status Check

Checks the AWS infrastructure running the instance (host hardware, power, networking).

If this fails → issue on AWS side.

Instance Status Check

Checks the OS inside the instance (kernel, networking, instance reachability).

If this fails → issue inside the instance.

Why You Might See 3/3 Status Checks

If you see 3/3 checks, it usually means an additional health check is enabled, commonly related to load balancing.

Additional Check Sources
1️⃣ Load Balancer Health Check

If the instance is registered behind Application Load Balancer or Network Load Balancer, an extra health check can appear.

Checks become:

System check

Instance check

ELB health check

Example:

3/3 checks passed
- System status check
- Instance status check
- ELB/Target group health check
2️⃣ Instance Reachability Monitoring

If integrated with monitoring services like Amazon CloudWatch or enhanced monitoring tools, a custom health check may appear.

Example Scenario

A web server behind an ALB:

Check	Meaning
System	AWS host hardware OK
Instance	OS reachable
Target Group	Application endpoint responding
Short Interview Answer

Normally EC2 shows 2/2 status checks: system and instance checks. If 3/3 checks appear, it usually indicates an additional health check such as a load balancer target group health check. This confirms that not only the infrastructure and OS are healthy, but the application behind the instance is also responding correctly.

--------------------------------------------------------------------------------------

Q.86) if an EC2 instance is hosted in the management account and we create an AMI backup, how can we share that AMI with child accounts?
Q.87) After sharing an AMI with child accounts and launching the instance, it is launching and terminating automatically. What could be the reason for this behavior?
Q.88) How can we identify if the AMI we shared has an issue that might be causing the instance to launch and terminate automatically?
Q.89) what are the steps involved in configuring a 3-tier architecture in AWS?
Q.90) if we are trying to download or save an RDS backup to an S3 bucket but RDS Is not connecting to S3, how can we troubleshoot this issue?
Q.91) What is Terraform and how does it differ from other IaC tools like Cloud Formation or ARM templates?
Q.92) what are Terraform providers and how do they work?
Q.93) Explain the difference between terraform plan and terraform apply.
Q.94) What is the purpose of the terraform state file?
Q.95) How do you manage secrets in Terraform?
Q.96) What are modules in Terraform and how do you use them?
Q.97) how does remote state work and why is it important?
Q.98) What are workspaces in Terraform?
Q.99) how do you handle terraform code in a CI/CD pipeline?
Q.100) what's the difference between count and for_each?
Q.101) What is Terraform, and what is its primary purpose?
Q.102) How does Terraform differ from other IaC tools like CloudFormation or Ansible?
Q.103) what are the key Terraform commands, and what do they do?
Q.104) what is a Terraform state file?
Q.105) What are Terraform providers, and why are they important?
Q.106) what are Terraform modules?
Q.107) How does Terraform workspaces, and when should you use them?
Q.108) How does Terraform handle importing existing infrastructure, and what are the limitations?
Q.109) What are Terraform provisioners, and when should you use them?
Q.110) what is drift detection in Terraform, and how can it be addressed?
Q.111) how would you implement a rolling update using Terraform for an application deployed in multiple instances?
Q.112) How do you handle resource dependencies in Terraform, and what is the role of implicit and explicit dependencies?
Q.113) How do you manage complex multi-cloud deployments with terraform?
Q.114) What are the taint and untainted commands in Terraform? How would you use them in a real-world scenario?
Q.115) What are zero-downtime deployments, and how can Terraform achieve them?
Q.116) How do you handle secrets management in Terraform, and what are the best practices?
Q.117) Explain the concept of remote state locking in Terraform and its importance in team collaboration.
Q.118) Explain terraform init, terraform plan, terraform apply, terraform validate, terraform output, terraform refresh, terraform input.
Q.119)How many plugins are installing in your project for Jenkins?
Q.120) how many components are there inVPC?
Q.121) how you worked on cloud watch?
Q122) share your screen and write a Dockerfile and explain how are building Docker images?
Q123) Can we use Load balancer for a single instance?
Q.124) How are you using pipeline triggering in Jenkins by using corncobs?
Q.125) explain your entire CI/CD, where's the blast radius?
Q.126) Difference between ALB and NLB, when does latency matter more than routing logic?
Q.127) Terraform taint vs terraform state rm, when would you use either?
Q.128) How does CoreDNS handle plugin chain resolution?
Q.129) Choose between EBS, EFS, and Fox for different workloads.
Q.130) Bash: find all .log files over 500 MB modified in the last 48hr.
Q.131) DB migration without downtime? Explain rollback contingency.
Q.132) Roll back a failed Helm release. What's your exact command chain?
Q.133) Secure vault secrets in GitHub Actions without hardcoding.
Q.134) Node is Ready, but kubelet logs are silent. What now?
Q.135) your autoscaler doubled the infrastructure costs. Logs are normal. What do you check?
Q.136) a prod pod restarts 8 times/hour. Logs look fine. What's your triage plan?
Q.137) Terraform drift detected. Who gets paged, and what's the fix sequence?
Q.138) Disaster recovery setup, what does it look like? When was it last tested?
Q.139) Simulate a DNS outage with the CoreDNS plugin disorder. How?
Q.140) Dockerfile: Multi-stage image with the Caching and minimised attack surface.
Q.141) Secure gRPC traffic across K8s namespaces.
Q.142) S3 bucket deleted by mistake, walk me through infra + IAM prevention.
Q.143) Helm worked in staging but broke in prod. Explain the failure class.
Q.144) App returns 200 OK, but functionality is broken, what did you monitor?
Q.145) How do you onboard juniors without overwhelming them?
Q.146) what's the worst production day you.'ve faced? What broke you?
Q.147) Ever shipped under pressure and regretted it?
Q.148) How do you protect engineering from non-technical deadlines?
Q.149) what does a "no blame" postmortem look like in your team?
Q.150) simulated a DNS cache corruption.
Q.151) Recovered a Terraform state loss.
Q.152) Diagnosed a 200/OK but broken service.
Q.153) Debugged CoreDNS plugin chain at midnight.
Q.154) CI/CD Pipelines: How do you run builds in multiple operating systems in a pipeline?
Q.155) Secret Management: How do you access Hashicorp Vault or any secret manager located in another AWS region?
Q.156) Kubernetes Basics: What are ConfigMaps and Secrets in Kubernetes?
Q.157) 502 Production issue: The pod is running, but logs show a 502 error - how would you debug it?
Q.158) CreshLoopBackOff Troubleshooting: How do you fix a CreshLoopBackOff issue in a pod?
Q.159) Missing Services: if a service doesn't appear in kubectl get services, what could be the problem?
Q.160) Missing Services: is a service doesn't appear in kubectl get services, what could be the problem?
Q.161) Cluster Monitoring: How do you check node-level resource consumption in Kubernetes?
Q.162) Container Crashes: What steps would you take if a container crashes immediately after starting?
Q.163) Explain Infrastructure as Code (IaC)?
Q.164) Difference between Docker and Kubernetes?
Q.165) How does CI/CD work?
Q.166) Explain service-level agreements (SLA), SLOs, and SLIs.
Q.167) How do you monitor system health?
Q.168) Difference between horizontal and vertical scaling?
Q.169) How do you set up high availability (HA)?
Q.170) Explain rolling deployments vs blue-green deployments?
Q.171) How do you troubleshoot production outages?
Q.172) Explain disaster recovery strategies?
Q.173) How do you expose a Kubernetes application to the outside world?
-> use an service of type LoadBalancer or configure an Ingress. LoadBalancers provide a direct public IP, while Ingress supports advanced routing, TLS termination, and centralised traffic management.
Q.174) What causes pods to go into a CreashLoopBackOff state?
-> Common reasons include incorrect image versions, missing environment variables, failing readiness/liveness probs, or insufficient resource limits. Checking logs with kubectl logs and describing the pod helps pinpoint the issue.
Q.175) How do you scale an application in kubernetes?
-> use kubectl scale to manually adjust replicas or define as HorizontalPodAutoscaler (HPA) for CPU/memory-based auto-scaling. This ensures apps can handle traffic spikes without downtime.
Q.176) what's the difference between a Deployment and a StatefulSet?
-> Deployments are ideal for stateless workloads where replicas are identical. StatefulStes are used for stateful apps (like databases) requiring stable network IDs, persistent storage, and ordered scaling.
Q.177) How do you secure sensitive data in Kubernetes?
-> Store credentials, API keys, and certificates in Secrets rather than ConfigMaps, Mount them as environment variables or volumes, and use encryption at rest with RBAC for strict access control.
Q.178) What's the best way to monitor Kubernetes workloads?
-> Integrate Prometheus for metrics collection, Grafana for visualization, and Alertmanager for proactive notifications, This trio gives end-to-end visibility into cluster health and app performance.
Q.179) How do you perform a rolling update in Kubernetes?
-> A Deployment supports rolling updates by gradually replacing old pods with maxUnavailable and maxSurge parameters.
Q.180) What's the purpose of a DaemonSet?
-> A DaemonSet ensures a copy of a pod runs on all (or selected) nodes in the cluster. It's commonly used for logging agents, monitoring tools, and security daemons.
Q.181) How do you handle persistent storage in Kubernetes?
-> Use PersistentVolumes(PV) and PersistentVolumeClaims (PVC) to abstract storage from pods. This allows apps to survive restarts and rescheduling without losing data.
Q.182) How do you check CPU status, and what is the expected output when you run that command?
Q.183) How do you check the status of a previously running process in your VM?
Q.184) what is the use go Docker Swarm?
Q.185) how do you log in to EKS cluster?
Q.186) How do you configure monitoring for your cluster?
Q.187) How do you patch your host without causing downtime?
Q.188) How do you change the instance type of your EKS cluster?
Q.189) Which protocol does Ansible work on?
Q.190) What is an Ansible playbook?
Q.191) What are your roles and responsibilities as a DevOps Engineer?
Q.192) What is the difference between a Deployment and a Statefulset in Kubernetes?
Q.193) when should you use a StatefulSet instead of a Deployment?
Q.194) Can you attach a volume to a Deployment? If yes, how is it different from a StatefulSet?
Q.195) What could cause a StatefulSet pod to fail when rescheduled to a different availability zone?
Q.196) How do PV/PVC behave across zones in EKS or Kubernetes in general?
Q.197) what is a Daemonset and when would you use it?
Q.198) What is a pod Disruption Budget (PDB) and how is it useful?
Q.199) How do you handle certificate rotation in on-perm kubernetes clusters?
Q.200) What are the challenges with scheduling pods in a multi-node, multi-AZ setup?
Q.201) How does the kubernetes scheduler decide where to place pods?
Q.202) What happens when a StatefulSet pod cannot mount its volume after moving to another node?
Q.203) What are common challenges faced while working with Terraform?
Q.204) How do you handle state file management in Terraform?
Q.205) How do you detect and resolve drift in Terraform-managed infrastructure?
Q.206) How do you manage secrets securely in Terraform?
Q.207) why should you use a remote backend for Terraform?
Q.208) What are all the possible ways to deploy an Nguni server on AWS?
Q.209) What are the pre-requisites for VPC peering between two VPCs?
Q.210) What problems occur when two VPCs have overlapping CIDR blocks?
Q.211) How can you enable communication between overlapping CIDR VPCs?
Q.212) What is a Transit Gateway and how does it help in VPC communication?
Q.213) How can a jump server be used in overlapping network scenarios?
Q.214) Can you explain transitive routing between VPCs A, B, and C?
Q.215) What CI/CD tools have you used in your current role?
Q.216) How are you integrating tools like SonarQube, Docker, and Trivy in your pipelines?
Q.217) How do you trigger a GitHub Actions workflow in another, repository?
Q.218) What is the purpose of repository_dispatcg in GitHub Actions?
Q.219)nHow would you trigger a CI/CD pipeline in Repo A from changes in Repo B?   
Q.220) Can you explain Lambda?
Q.221) What is auto scaling groups?
Q.222) What are the different storage classes in S3?
Q.223) How does S3 ensure data durability?
Q.224) Difference between IAM role and user?
Q.225) What is VPC and what are the components of VPC?
Q.226) Difference between Public and Private Subnet?
Q.227) How would you design a highly available web-app in AWS?
Q.228) You need to migrate a large on permises database to AWS. What should you do?
Q.229) EC2 instances cannot connect to the internet. What could be the reason?
Q.230) Explain Horizontal and Vertical Scaling.
Q.231) How do you secure data at-rest and in-transit in AWS?
Q.232) can you explain about cloud formation?
Q.233) can you implement MFA in AWS?
Q.234) How does S# Encryption work?
Q.235) Explain VPC - Peering?
Q.236) Can you explain the logic volume manager (LVM) in linux?
Q.237) Explain Grep command in Linux?
Q.238) Explain Network Commands in Linux?
Q.239) How do you manage system service in Linux?
Q.240) Explain Disk Usage in Linux?
Q.241) in AWS, if the storage is full, how will you extend the storage?
Q.242) in the AWS WC2 instance, I need to increase the size 8GB to 16 GB, how would you do that?
Q.243) Explain Etcd in Kubernetes?
Q.244) Explain Replica set in Kubernetes?
Q.245) How does kubernetes handle networking?
Q.246) How do you monitor kubernetes Cluster?
Q.247) What is Daemon set in Kubernetes?
Q.248) Difference between daemon set and statefulset?
Q.249) How do you troubleshoot pod in Kubernetes?
Q.250) What is the difference between terraform import and terraform taint?
Q.251) How do you manage secrets in Terraform without hardcoding them?
Q.252) What's the difference between count and for_each? Give a real-world use case.
Q.253) How do you handle drift detection in Terraform?
Q.254) What is a Terraform remote backend, and why is it important?
Q.255) How do you manage multiple environments (dev, staging, prod) in Terraform?
Q.256) Difference between local-exec and remote-exec provisioners.
Q.257) How do you safely roll back infrastructure changes after a failed deployment?
Q.258) Explain terraform refresh vs terraform plan.
Q.259) How do you write reusable Terraform modules?
Q.260) what is Amazon VPC?
Q.261) What is the difference between a default VPC and a custom VPC?
Q.262) What is the maximum number of VPCs per AWS region?
Q.263) What is a subnet in VPC?
Q.264) What is the difference between a public subnet and a private subnet?
Q.265) What is a route table in VPC?
Q.266) What is an internet Gateway (IGW)?
Q.267) What is a NAT Gateway and how is it different from a NAT Instance?
Q.268) What Is the difference between Security Groups and NACLs?
Q.269) What is a VPC Endpoint and what ate its types?
Q.270) What is the difference between a Gateway Endpoint and an Interface Endpoint?
Q.271) What is VPC Peering?
Q.272) What is AWS Transit Gateway and how is it different from VPC Peering?
Q.273) What is an Elastic IP (EIP) in VPC?
Q.274) What are flow Logs in VPC and why are they used?
Q.275) What is the difference between IPv4 CIDR and IPv6 CIDR in VPC?
Q.276) What is the maximum number of IP addresses in VPC subnet?
Q.277) What are the default components created when you launch a new VPC?
Q.278) how do you find the 5 largest files in a directory (e.g., /var/log)?
-> find /var/log -type f -exec du -h {}+ | sort -rh | head -n 5
Q.279) How to check which process is using the most memory or CPU?
-> ps aux --sort=-%mem | head
Q.280) How do you monitor live logs of a running application?
-> tail -f /var/log/syslog
Q.281) how do you check which ports are open and which process is using them?
-> sudo netstat -tlnup
Q.282) How do you find all files modified in the last 24 hours?
-> find /home -type f -mtime -1
Q.283) How do you check disk usage of all directories under /var?
-> du -h --max-depth=1 /var
Q.284) How do you extract specific columns from a file (e.g., access.log)?
-> awk '{print $1, $9}' access.log
Q.285) How do you replace a word in a file without opening it?
-> sed -I 's/oldword/newword/g' file.txt
Q.286) How do you find the number of times the word "error" appears in a log file?
-> grep -o "error" /var/log/syslog | wc -l
Q.287) How do you check which services are running or failed on a system?
-> systemctl list-units --type=service --state=running
Q.288) Explain your Day to Day activities?
Q.289) Do you have hands-on in Linux? Ig yes, which platform?
Q.290) What is the latest version of ubuntu?
Q.291) I have index.html on GitHub, I need to push it into was. The requirement is one load balancer is required and we provide index.html inn GitHub repo and we provide username and token. How to achieve this?
Q.292) what is dependincies resources for the IP?
Q.293) What is the command to connect EC2 instance?
Q.294) How to implement internet gateway?
Q.295) How to configure internet gateway to route table?
Q.296) what is the exact command to log in the instance and what kind of authentication will you use? Do you need password to provide or something else?
Q.297) what is the exact command to log in the instance and what kind of authentication will you use? Do you need password to provide or something else?
Q.298) in which path will you create index.html in server?
Q.299) Explain most common linux commands?
Q.300) How to check one running process?
Q.301) How to list running process?
Q.302) you need to find one particular process id and how to kill that? Is it possible in single command?
Q.303) How to check disk usage?
Q.304) How to find free memory?
Q.305) How do you achieve and compress directory in Linux?
Q.306) Chmod 755 means?
Q.307) if I provide chmod 755 means, what exactly will happen?
Q.308) what is chown?
Q.309) How to list all ssh users in Linux?
Q.310) if one apache server is running, I need to check logs? Where do I check? In which directory?
Q.311) what kind of logs can see in /var/log?
Q.312) purpose of Grep command?
Q.313) I need to grep keyword: linux. Expect the keyword linux, I need to list out all the lines from the linux.txt file. Which flag will you used with grep?
Q.314) Difference between "cron" and "at" ?
Q.315) How to schedule activity in Linux and you need to run a script every 5 minutes?
Q.316) Explain Git Pull and Git Fetch?
Q.317) How do you resolve merge conflict?
Q.318) How to create custom image in Docker?
Q.319) Elaborate Docker file.
Q.320) what is the prerequisites of "docker build" command?
Q.321) How do copy file from the container to host?
Q.322) what if we put "COPY." ? what will happen in background? What is the source and destination here?
Q.323) what is Route 53?
Q.324) is it possible to purchase a domain in route 53?
Q.325) what is the configuration file of prometheus?
Q.326) where can I setup the alerting? In prometheus or Grafana?
Q.327) I want to configure one alerting disk usage of one of the server reaches 80%, it should alert sms? Where can I do these configuration?
Q.328) How to generate token in GitHub? Explain.
Q.329) What is a VPC?
Q.330) What is a Subnet?
Q.331) What is an Internet Gateway?
Q.332) What is a NAT Gateway?
Q.333) What are Route Tables?
Q.334) What is a Security Group?
Q.335) What is a Network ACL (NACL)?
Q.336) What is an Elastic IP?
Q.337) What is a VPC Peering Connection?
Q.338) What is a Load Balancer in AWS?
Q.339) What is Route 53?
Q.340) What is AWS Direct Connect?
Q.341) What is a Transit gateway?
Q.342) How do you secure network traffic in the cloud?
Q.343) What are VPC Flow Logs?
Q.344) What is a PrivateLink or VPC Endpoint?
Q.345) What is CIDR in VPC?
Q.346) What is the difference between Public and Private Subnets?
Q.347) What is a Bastion Host?
Q.348) How do you connect on-premises to AWS Cloud?
Q.349) What is Elastic Load balancing (ELB)?
Q.350) What are the different types of load balancers in AWS?
Q.351) What is an Availability Zone and how does it affect networking?
Q.352) What is DNS failover in Route 53?
Q.353) What is an Elastic Network Interface (ENI)?
Q.354) What is IP addressing in cloud networking?
Q.355) What is peering vs transit gateway?
Q.356) What is the difference between NACL and Security Groups?
Q.357) What is the role of a default VPC?
Q.358) What is a VPC endpoint and its types?
Q.359) What is AWS Global Accelerator?
Q.360) What is the use of CloudFront in networking?
Q.361) What are ingress and egress traffic?
Q.362) What is the difference between IGW and NAT Gateway?
Q.363) What are best practices for securing a VPC?
Q.364) What is hybrid connectivity in cloud?
Q.365) How does VPN connection work in AWS?
Q.366) What is AWS PrivateLink?
Q.367) How do you troubleshoot VPC connectivity issues?
Q.368) What is the difference between private IP, public IP, and elastic IP?
Q.369) What is Linux patching?
Q.370) Why is patching important in Linux systems?
Q.371) What are the different types of patches in Linux?
Q.372) How do you check for available patches or updates in Linux?
Q.373) What command is used to update all packages in Red Hat or CentOS?
Q.374) What command is used to update all packages in Ubuntu or Debian?
Q.375) How do you check the current kernel version before and after patching?
Q.376) How can you check when the system was last patched?
Q.377) what is the difference between a security patch and a kernel patch?
Q.378) How do you apply only security updates on Linux?
Q.379) What tools can be used for automated patching in Linux>
Q.380) How can you perform patching using yum or dnf?
Q.381) How do you exclude certain packages from being updated during patching?
Q.382) What is yum-cron or dnf-automatic used for?
Q.383) What is the difference between yum update and yum upgrade?
Q.384) How do you perform a patch rollback if an update causes issues?
Q.385) How do you patch multiple servers at once?
Q.386) What precautions should be taken before applying patches?
Q.387) What is kernel live patching?
Q.388) What tools are used for live patching in Linux (e.g., patch, ksplice)?
Q.389) How do you schedule patching with minimal downtime?
Q.390) What's the difference between manual patching and automated patching?
Q.391) How do you verify that patches have been applied successfully?
Q.392) What logs can you check after patching for any errors?
Q.393) What's your approach if a server fails to boot after a kernel patch?
Q.394) How do you test patches before applying them in production?
Q.395) What is the importance of backups before patching?
Q.396) How do you notify users or teams about patching activities?
Q.397) How do you manage patching in large Linux environments?
Q.398) What's the difference between OS patching and application patching?
Q.399) What is the difference between security group and NACL?
Q.400) How to recover an EC2 instance if the key pair is lost?
Q.401) VPC Peering vs Transit Gateway?
Q.402) How to troubleshoot an EC2 server?
Q.403) Can you tell me in how many ways we can connect to a private instance inside a VPC?
Q.404) What is Kubernetes architecture?
Q.405) Deployment vs StatefulSet?
Q.406) Explain your project infrastructure?
Q.407) Write a Terraform script to create EC2 instance?
Q.408) What would happen if the state file deleted?
Q.409) How can you set up VPC, subnet, and route table for a gaming application and leaderboard having a database as well with minimum latency?
Q.410) What would be the High Availability and DR strategies of it?
Q.411) How would you manage billing for cloud when there is high traffic load?
Q.412) What is Kubernetes Architecture?
Q.413) Difference between Pod, Node, and Cluster?
Q.414) Horizontal Pod Autoscale vs Vertical Pod Autoscaler?
Q.415) If Developers say there is a latency issue, how would you reduce the latency to Kubernetes pods?
Q.416) if node goes down, what would happen to the pod?
Q.417) What would be your action for it?
Q.418) What are ConfigMaps and Secrets?
Q.419) Your pod is stuck in CreshLoopBackoff. How do you debug and fix it?
Q.420) How do you perform zero-downtine deployments in Kubernetes?
Q.421) Your service is not accessible externally - where do you start troubleshooting?
Q.422) Explain how you'd handle a failed rollout during a deployment?
Q.423) How would you optimise resource requests and limits in a production cluster?
Q.424) How do you secure secrets in Kubernetes?
Q.425) A node went down suddenly what happens to the pods running on it?
Q.426) How do you handle database credentials rotation in Kubernetes?
Q.427) What's your strategy for backup and restore in a cluster?
Q. 428) How do you implement auto scaling when traffic fluctuates heavily?

---------------------------------------------------------

Q. 429) what is the command for Kubernetes Roll Back command ?
-> kubectl rollout undo deployment/nginx-deployment
-> kubectl rollout history deployment/nginx-deployment (last successfull)
-> kubectl rollout undo deployment/nginx-deployment--to-revision=2 (roll back to specific version)

---------------------------------------------------------

Q. 430) What is the folder structure of Ansibe role?
-> Roles can be reused across different playbook and projects. When you create a role using the command ansible-galaxy init <role_name>, it generates a directory structure that looks like below:

1) defaults/ -> contains the default variables for the role. These variables have the lowest priority in the variable precedence hierarchy.

2) files/ -> file.txt: a static text file that can be copied to the target system. Example content:
This is a static file used for demonstration.
Script.sh: A shell script to be executed on the target system

3) handlers/ -> contains handlers, which are tasks triggered by the notify directive. 

-------------------------------------------------------------------------------------

Q. 431) what are the components of Docker?
-> Docker image, docker File, docker hub, docker registry, docker engine.
- The Client sends Docker commands (docker build, docker run, etc.)
- The Daemon receives these commands and performs container operations.
- The REST API is the interface enabling this communication.
- Tasks include create, start, stop, and delete containers.

--------------------------------------------------------------------------------------

Q. 432) What is an AWS Region?
Q. 433) Why does AWS provide multiple Regions?
Q. 434) How many AWS Regions are Available today?
Q. 435) Name any 5 AWS Regions?
Q. 436) Which region is closest to your location?
Q. 437) Why is Region selection important?
Q. 438) Do all AWS services exist in every Region?
Q. 439) What is the naming format of AWS Regions?
Q. 440) Difference between Region and Availability Zone?
Q. 441) What factors should you consider when selecting Region?
Q. 442) What is latency and how do Regions affect it?
Q. 443) what is data sovereighty?
Q. 444) What is data residency?
Q. 445) why is us-east-1 (N. Virginia) heavily used?
Q. 446) why is Mumbai (ap-south-1) popular in india?
Q. 447) What is the default Region in AWS management COnsole?
Q. 448) how do you change the default Region?

--------------------------------------------------------------------------

Q. 449) Can a Region go down in aws?
-> yes, an entire AWS Region can go down, but it is very rare. AWS designs Regions to be highly isolated, yet large-scale outages have happened.
- why Region Can Go Down
- A Region may become unavailable due to:
. Power or networking failures
. Software bugs in core AWS services
. Human error
. Natural disasters
. cascading failures across AZs.
---------------------------------------------------------------------------

Q. 450) What happens if an entire Region fails in AWS?
-> if an entire AWS Regios fails, all reagional services go down, Recovery depends on pre-configured multi-region DR using Route 53, cross-region replication, and standby or active-active architectures.
- DevOps Best Practice Checklist
. Multi-AZ in NOT enough
. Always plan multi-region Disaster Recovery for critical apps
. Define RTO & RPO
. Test DR using failover drills
. Keep IaC ready for fast recovery

-----------------------------------------------------------------------------

Q. 451) Zero Downtime K8s Upgrades?
-> Zero-downtime kubernetes upgrades are achieved using deployments, readiness probes, PodDisruptionBudgets, graceful shutdowns, and node-by-node upgrades with proper load balancing.

--------------------------------------------------------------------------------

Q. 452) How to upgrade kubernetes clusters without downtime?
-> use rolling updates via Deployment strategies + multiple node pools (drain/cordon old nodes first)

------------------------------------------------------------------------------------

Q. 453) What packages install while we pass kubeadm init command in kubernetes?
-> A container images are pulled, By default, kubeadm pulls these images from registry.k8s.io:
kube-apiserver
kube-controller-manager
kube-scheduler
kube-proxy
etcd
pause
coredns

-------------------------------------------------------------------------------------

Q. 454) if kubernetes node not ready state then how to troubleshoot and which service need to check inside the node?
-> when a kubernetes node is in NotReady state, it usually means the kubelet cannot properly communicate with the control plane or the container runtime / CNI is broken.
Below is a practical, DevOps-ready troubleshooting flow
1) First check node condition (from control plane)
-> kubectl get nodes
   kubectl describe node <node-name>
2) Most important service to check on the node. (kubelet service)
-> systemctl status kubelet
3) check container runtime (very common cause, depending on runtime)
-> systemctl status containerd
-> systemctl status docker
4) Check CNI (Networking), if CNI is not installed or broken
-> ls /etc/cni/net.d/
5) Check CNI pods
-> kubectl get pods -n kube-system
6) Check node resources (Pressure conditions)
-> kubectl describe node <node-name>
7) also verify disk
-> df -h
8) Restart sequence (safe order)
-> systemctl restart containerd
-> systemctl restart kubelet

---------------------------------------------------------------------

Q. 455) 1.What is Linux-
Linux is an open-source operating system based on Unix.We use it for servers, cloud, DevOps, and embedded systems.It is stable, secure, and highly customizable. 

----------------------------------------------------------------------

Q. 456)  What is the Linux kernel?
The kernel is the core of Linux
It manages CPU, memory, processes, devices, and system calls.Applications interact with hardware through the kernel.

------------------------------------------------------------------------------

Q. 457) What is the difference between Linux and Unix?
Unix is proprietary, while Linux is open-source.Linux is free and widely used in servers and cloud.Unix is mainly used in enterprise systems 

------------------------------------------------------------------------------

 Q. 458) What is a shell?
The shell is a command-line interface between user and OS.We use it to run commands and scripts Common shells are bash, sh, and zsh. 

-----------------------------------------------------------------------------

Q. 459) What is a process?
A process is a running instance of a program.Each process has a unique PID.We manage processes using ps, top, and kill.

----------------------------------------------------------------------------

Q. 460) What is the difference between soft link and hard link?
A hard link points to the same inode as the file.A soft link is like a shortcut to the file pathSoft links break if the original file is deleted 

------------------------------------------------------------------------------

Q. 461)  What are file permissions in Linux?
Linux uses read (0), write (w), and execute(x)permissions.Permissions apply to owner, group, and others.
We manage them using the chmod command. 

-------------------------------------------------------------------------------

Q.462) Why is execute permission needed on a directory?
Execute permission allows us to access the directory.Without it, we cannot enter or list files inside Read permission alone is not enough.

--------------------------------------------------------------------------

Q. 463) What is /etc/passwd?
It stores user account information.
It includes username, UID, GlD, and home directory.Passwords are not stored here directly. 

----------------------------------------------------------------------------

Q. 464) Difference between Jenkins vs GitHub Actions - choosing the right CI/CD Tool?
-> The biggest difference: jenkins is a highly customizable, self-hosted automation server, while GitHub Actions i s a fully managed, GitHub-native CI/CD solution.
Here'a a quick breakdown
- Jenkins -> Self-hosted and highly flexible Massice plugin ecosystem Full control over infrastructure and pipelines Requires setup, maintenence, and upgrades best of complex, enterprise-level workflows.

- GitHub Actions -> Fully managed and cloud-native Deeply integrated with GitHub repositories Simple YAML-based workflows Minimal maintenance effort ideal for fast-moving teams and GitHub-first projects

Key Takeaway:
Use Jenkins when you need deep customization and infrastructure control. Choose GitHub Actions when simplicity, speed, and GitHub integration matter most.

-------------------------------------------------------------------------------

Q. 465) How do you check disk usage?
We use df -h to check filesystem usage
du -sh helps check directory size
This is useful when servers run out of space.

-----------------------------------------------------------------------------------

Q. 466) How to reverse a list in python?
-> 1) my_list = [1,2,3,4,5]
      my_list.reverse()
      print(my_list)

     o/p -> [5,4,3,2,1]
 
   2) my_list = [1,2,3,4,5]
      reverse_list = mylist[::-1]
      print(reverse_list)

    o/p -> [5,4,3,2,1]

------------------------------------------------------------------------------------

Q. 467) write a sample code for remote state management in terraform?
-> terraform {
  backend "s3" {
    bucket         = "my-terraform-state-bucket"
    key            = "path/to/myproject.tfstate"
    region         = "us-east-1"
    dynamodb_table = "terraform-lock-table"
    encrypt        = true
  }
}

-------------------------------------------------------------------------------------

Q. 468) from the using list [1,2,3,4,5] i want to get the output only [5,3,1] in python, how i can achieve?
-> my_list = [1,2,3,4,5]
   result = mylist[::-1][::-2]
   print(result)

--------------------------------------------------------------------------------

Q. 469) Deployment Strategies Every DevOps Engineer should know?
-> 1) Recreate ->
    - stop old version -> deploy new one
    - Downtime involved
    - Best for: Dev/Test, non-critical apps

   2) rolling update -> 
    - Gradual replacement of instances/pods
    - No Downtime
    - Best for: Kubernetes, stateless apps

   3) Blue-Green ->
    - Two identical environments (Blue = live, Green = new)
    - Instant traffic switch & rollback
    - Best for: critical production systems

   4) Canary ->
    - New version released to a small % of users first
    - safest rollout with real user validation
    - Best for: High-traffic apps with strong monitoring

---------------------------------------------------------------

Q. 470) How do you secure data at rest and in transit in AWs?
Q. 471) Explain the difference between AWS S3 Standard and S3 Glacier?
Q. 472) How does AWS S# versioning work?
Q. 473) What is AWS Elasticache?
Q. 474) Explain the concept of a bastion host?
Q. 475) How do you implement high availability in AWS?
Q. 476) What is AWS Direct Connect?
Q. 477) What are AWS Managed Services?
Q. 478) What is AWS Config?
Q. 479) How do you set up cross-region replication in S3?
Q. 480) Explain AWS KMS?
Q .481) What is Amazon Redshift?
Q. 482) How does AWS handle data encryption?
Q .483) What is Amazopn EFS?
Q. 484) Explain AWS Elastic Transcoder?
Q. 485) What is AWS CodePipeline?
Q. 486) How do you implement disaster recovery in AWS?
Q. 487) What is AWS OpsWorks?
Q. 488) What is AWS Step Functions?
Q. 489) Explain the difference between Spot instance and Reserved Instances?
Q. 490) What is Amazon SWF?
Q. 491) How do you secure an AWS API Gateway?
Q. 492) What are placement Groups in AWS?
Q. 493) What is AWS CodeDeploy?
Q. 494) How does Amazon Athena work?
Q. 495) What is AWS showball?
Q. 496) Explain the concept of AWS CloudHSM?
Q. 497) What is AWS X-Ray?
Q. 498) How do you manage secrets in AWS?
Q. 499) Explain AWS Systems Manager?
Q. 500) How/do you design, manage, and test backup and disaster recovery strategies for production system?
Q. 501) What principles and AWS services do you use to design for failure in cloud architectures?
Q. 502) Explain the Linux file system hierarchy. What is /etc, /var, /home, /usr?
Q. 503) what is the difference between an absolute and relative path?
Q. 504) How do you install, remove, or upgrade packages?
Q. 505) How do you schedule tasks using cron?
Q. 506) What is the use of /etc/hosts and /etc/resolv.conf file?
Q. 507) What is the purpose of shebang?
Q. 508) What are sticky bits, SUID, and SGID?
Q. 509) what is the difference between locate and find a command?
Q. 510) What tools are used for network debugging?
Q. 511) what is the port no. of ssh? can we change it ? if yes, how ?
Q. 512) A server is swapping heavily How do you identify memory-hungry processes and stabilize the server?
Q. 513) What is the difference between kill, killall, and pkill?
Q. 514) what is the main difference between yum and rpm command, as both are used to install packages?
Q. 515) Your server is not responding on SSH, but it pings. What do you check next?
Q. 516) A system update caused your kernel to break. How do you boot using an older kernel?
Q. 517) How you can increase the filesystem?
Q. 518) A file system is mounted read-only unexpectedly. How do you fix it?
Q. 519) Explain partitions and file systems. How you can create a filesystem and format a disk?
Q. 520) what are the different types of raid in linux?
Q. 521) df -h command is in hung state, what are the possible reason of it and how you will resolve it ?
Q. 522) How do you fix 'Permission Denied' errors on EC2? 
Q. 523) How do you verify file ownership and permissions? 
Q. 524) How do you change directory ownership for Jenkins deployments? 
Q. 525) How do you identify files recently modified by root user? 
Q. 526) How do you restore permission of system files accidentally changed? 
Q. 527) How do you check sudo privileges of a user? 
Q. 528) How do you give only read-only access to a file? 
Q. 529) How do you check which user modified a file last? 
Q. 530) How do you allow non-root user to run Docker commands? 
Q. 531) How do you prevent unauthorized permission changes? 
Q. 532) How do you verify if Nginx or Apache is running after deployment? 
Q. 533) How do you restart a failed service automatically? 
Q. 534) How do you debug 'address already in use' errors? 
Q. 535) How do you verify deployed app files on EC2? 
Q. 536) How do you clear old build artifacts before deployment? 
Q. 537) How do you validate environment variables for app startup? 
Q. 538) How do you roll back a failed deployment? 
Q. 539) How do you test app startup script manually? 
Q. 540) How do you check if EC2 has correct IAM role access? 
Q. 541) How do you confirm health of EC2 target behind ALB? 
Q. 542) How do you check EBS volume attachment and size? 
Q. 543) How do you identify which mount point is using which EBS? 
Q. 544) How do you increase EC2 disk size safely? 
Q. 545) How do you create a snapshot of EBS volume? 
Q. 546) How do you mount an unmounted EBS volume? 
Q. 547) How do you troubleshoot 'read-only file system' errors? 
Q. 548) How do you check free space inside a specific folder? 
Q. 549) How do you archive old log files automatically? 
Q. 550) How do you sync data between two EC2 instances? 
Q. 551) How do you back up EC2 files to S3 bucket?
Q. 552) Your EC2 instance is running but application is not accessible. How will you troubleshoot?
Q. 553) A sudden spike in traffic caused application downtime. How do you handle it in AWS?
Q. 554) Your EC2 instance CPU is constantly at 100%. What steps will you take?
Q. 555) An Auto Scaling group is not launching new instances. How will you debug?
Q. 556) Users cannot access S3 objects publicly. What could be wrong?
Q. 557) Sensitive data was accidentally made public in S3. What immediate actions will you take?
Q. 558) Your RDS database is slow. How do you troubleshoot?
Q. 559) A Multi-AZ RDS failed over. What happens next?
Q. 560) Application works in one AZ but not in another. How will you debug?
Q. 561) Load balancer is healthy but traffic is not reaching EC2. Why?
Q. 562) How do you implement zero-downtime deployment in AWS?
Q. 563) How do you design blue-green deployment using AWS services?
Q. 564) How do you secure EC2 instances without storing credentials?
Q. 565) How do you provide cross-account access securely?
Q. 566) How do you restrict developers from deleting production resources?
Q. 567) How do you monitor and alert for AWS infrastructure failures?
Q. 568) CloudWatch alarm didn’t trigger. How will you troubleshoot?
Q. 569) AWS bill suddenly increased. How do you investigate?
Q. 570) How do you design disaster recovery for a critical application?
Q. 571) How do you decide between multi-AZ and multi-region?
Q. 572) How do you prevent DDoS attacks in AWS?
Q. 573) How do you detect compromised IAM credentials?
Q. 574) How do you audit user actions across AWS accounts?
Q. 575) How do you encrypt data at rest and in transit in AWS?
Q. 576) How do you secure VPC communication?
Q. 577) How do you expose a private application securely to the internet?
Q. 578) How do you migrate on-prem workloads to AWS?
Q. 579) How do you perform rollback after a failed deployment?
Q. 580) How do you handle region-wide AWS outages?
-> Handling region-wide AWS outages is about designing for failure before it happens, not reacting when it happens. this is a senior DevOps / architect-level topic.
First: Accept the reality -> if an aws region goes down:
- multi-AZ will NOT help
- only multi-region architecture saves you
- you must design based on business RTO/RPO

1) Decide RTO & RPO (most important)

2) Common multi-region strategies
A) Active - passive (most used)
- 
--------------------------------------------------------------------------

Q. 581) How do you design AWS architecture for high availability and cost optimization?
-> A) High Availability Architecture (HA)
1) Multi-AZ by default (must)
Always design workloads to run in at least 2 Availability Zones.
Services ->
- ALB/NLB -> Multi-AZ
- Auto Scaling Groups -> Multi-AZ
- RDS -> Multi-AZ
- EFS -> Multi-AZ
- ElastiCache -> Multi-AZ
- NAT Gateway -> 1 per AZ (important)

2) Stateless Application layer ->
Keep compute stateless, dtore state externally:
- Session -> Redis / DynamoDB
- Files -> S3 / EFS
- Config -> SSM Parameter Store / Secrets Manager

This allows:
- Auto scaling
- fast recovery
- Zero downtime deploys

3) Auto Scaling everywhere ->
use ASG / ECS service Auto Scaling / EKS HPA
- Scalte out during peak
- Scale in during low traffic

4) Multi-region (for critical systems only)
For business-critical systems:
- Riute53 health checks
- Aurora Global DB
- S3 CRR (cross-region replication)
- only if RTO/RPO is strict (multi-region is expensive)

B) Cost Optimization(without breaking HA)
1) Right compute choice

2) Use Spot instances safely ->
- use mixed instances policy
- spot for stateless / batch
- on-demand for baseline

3) Storage optimization
- S3 lifecycle rules -> IA / Glacier
- gp3 instead of gp2
- Delete unused EBS snapshots
- Right-size RDS storage

4) Database cost control
- Use Aurora Serverless v2 for spiky workloads
- use read replicas instead of scaling up
- turn off dev/test DB at night (automation)

5) Network cost optimization
- Use VPC endpoints (avoid NAT traffic)
- Minimize cross-AZ traffic
- CloudFront for content delivery
- Avoid unnecessary inter-region traffic

-----------------------------------------------------------------------------------------

Q. 582) What happens if terraform plan shows no changes but infrastructure was modified outside Terraform?
-> Terraform won't detect the drift until you run terraform refresh or terraform plan -refresh-only. This can lead to unexpected behavior when making future changes.

-------------------------------------------------------------------------------------

Q. 583) What happens if multiple team members run terraform apply simultaneously?
-> State file locking fails, risking corrupted state and inconsistent infrastructure. One process succeeds while others error out, potentially leading to drift if not managed properly.

-------------------------------------------------------------------------------------

Q. 584) What happens if a resource fails halfway through a terraform apply?
-> Terraform leaves successfully created resources running but marks the state as tainted. Subsequent apply operations will attempt to recreate failed resources, but you're left in partial state.

---------------------------------------------------------------------------------------

Q. 585) What happens when AWS API rate limits are hit during a large terraform apply?
-> Operations fail with throttling errors. Terraform retries a few times then fails the apply. Resources created before the limit was hit remain, creating partial deployments.

--------------------------------------------------------------------------------------

Q. 586) What happens if your state file is accidentally deleted?
-> Terraform loses track of all managed infrastructure. On next apply, it will attempt to recreate everything from scratch, potentially causing conflicts with existing resources

----------------------------------------------------------------------------------

Q. 587) What happens if you delete a resource definition from your configuration?
-> On next apply, Terraform will destroy that resource in your infrastructure unless you use terraform state rm to remove it from state first or use lifecycle { prevent_destroy = true }.

---------------------------------------------------------------------------------

Q. 588) What happens if a provider API changes between Terraform versions?
-> You may encounter compatibility issues and failed plans/applies. Resources might need to be rebuilt or configurations updated to match new API requirements.

-----------------------------------------------------------------------------------
Q. 589) What happens if you have circular dependencies in your Terraform modules?
-> Terraform will fail to initialize or plan with dependency cycle errors. You'll need to refactor your module structure to break the circular references.

-------------------------------------------------------------------------------

Q. 590) What happens if you exceed AWS service quotas during deployment?
-> Resources will fail to create with quota exceeded errors. Terraform marks them as failed, and you'll need to request quota increases before retrying the apply.

---------------------------------------------------------------------------------

Q. 591) write a python code to find the missing no from the list [1,2,4,5,6] answer will be 3?
-> nums = [1,2,4,5,6]
   for i in range(1,max(nums)):
   if i not in nums:
   print (i)
   break

-------------------------------------------------------------------------------------

Q. 592) how to communicate with vpc to vpc in aws?
-> VPC Peering is the solution for communicate with vpc to vpc in aws.
- create a 1 to 1 private connection between two vpc.
- Traffic stays on AWS private netwoek (no internet)
- Works same region or cross region
- Works cross account

Steps ->
- Create VPC peering connection
- Accept peering request (if cross-account)
- Update route tables
- Update security groups / NACLs

---------------------------------------------------------------

Q. 593) What is the events in GitHub action?
-> In GitHub Actios, events are the triggers that kick off workflows. These events are specific activities in your repository that lead to the execution of actions.

- Common GiHub Action Event Types ->
- Push:- Triggered when code is pushed to the repository.
- Pull Request :- Activated when a pull request is created, opened, or updated.
- Release:- Occurs when a release is created or updated.
- Label:- Happens when a label is applied or removed.
- Issues:- Fired when an issue is opened, edited, or closed.
- Milestone:- Triggered when a milestone is added or updated.

------------------------------------------------------------------

Q. 594) What is Runners in GitHub Actions?
-> Runners are the virtual machines or environments where your GitHub Actions workflows execute. GitHub provides runners for various operating systems, such as Ubuntu, Window, and MacOS. You can specify the types of runner to use for each job in the workflow by setting the runs-on option.

jobs:
  build:
    runs-on: ubuntu-latest

------------------------------------------------------------------------------

Q. 595) How to manage Secrets in GitHub Action?
-> you must never hardcode sensitive values like API keys, passwords, or cloud credentials in your YAML files. GitHub Actions provides Encrypted Secrets for this.
- How -> You store them in your repository settings (settings > Secrets and variables > Actions).
- Usage -> You access them in your workflow using the secrets context:

step:
  - name: Login to Docker Hub
    uses: docker/login-action@v3
    with:
      username: ${{ secrets.DOCKERHUB_USERNAME }}
      password: ${{ secrets.DOCKERHUB_TOKEN }}

--------------------------------------------------------------------------

Q. 596) What is Self-Hosted Runners?
-> while GitHub-hosted runners are easy, self-hosted runners are a key system design choice for many companies.
why?
- Security & Network Access :- The runner can be placed inside your private network (e.g. your private VPC in AWS). This allows your CI/CD pipeline to securely access private databases, internal artifacts, or kubernetes clusters that are not exposed to the public internet.

- Custom Hardware :- you can use runners with specific hardware, like powerful GPUs for machine learning, or ARM-based CPUs.

- Cost & Performance:- For organizations with very high build volumes, it can be more cost-effective to manage their own fleet of runners.

--------------------------------------------------------------------------

Q. 596) What is Caching Dependencies in GitHub Action?
-> CI/CD jobs run in clean environments, meaning they have to download dependencies (like node_modules or Maven packages) every single time. This is slow and wasteful. you v=can use actions/cache to store these dependencies and restore them on subsequent runs.

------------------------------------------------------------------------

Q. 597) What is the Matrix Builds in GitHub Action?
-> To ensure your system is reliable, you might need to test it across multiple platforms, language versions, or configurations. A matrix strategy lets you run the same job multiple times with different parameters.

-----------------------------------------------------------------------

Q. 598) Why use GitHub Actions?
GitHub Actions offers several advantages over traditional CI/CD tools
- Semless Integration:- Works natively within GitHub, reducing the need for external CI/CD services.
- Event-Driven Automation:- Triggers workflows based on repository events like pushes, pull requests, and issues.
- Parallel Execution :- Run nultiple jobs concurrently to speed up CI/CD pipelines.
- Customizable Workflows:- Defines workflows in YAML files to suit specific project needs.
- Supports Multiple Runners :- Works with GitHub-hosted and self-hosted runners for diverse environments.
- Marketplace for Actions:- Utilize pre-built actions from the GitHub Marketplace to simplify workflow creation.

---------------------------------------------------------

Q. 599) What is the Use Cases of GitHub Actions?
-> GitHub Actions can be applied in various scenarios.
- CI/CD Pipelines:- Automate testing, building, and deployment.
- Automated Code Reviews:- Run static analysis and linting tools.
- Scheduled Jobs :- Automate routine tasks such as backups.
- Security Scans:- Detect vulnerabilities using security tools.
- Notification Triggers :- Send alerts to Slack, email, or Discord.

-------------------------------------------------------------------------

Q. 560) What will happen if core dns is down?
-> If CoreDNS is down in Kubernetes, the main impact is DNS resolution failure inside the cluster.
1) service-to-service communication Breaks -> pods cannot resolve service names, Microservices cannot talk to each other, Applications may throw error like: Name or service not known, Temporary failure in name resolution.

2) New pods may fail to start properly -> if an application depends on: Detabase hostname, External APIs(via DNS), Internal services.
- The pod may: CrashLoopBackOff, Stay in Running but application fails internally.

3) External Domain Resolution Fails ->
if your pods call: google.com, payment gateway APIs, S3 endpoints, External SaaA services, All external DNS lookups will fail.

4) Running Connections Continue Working -> 
Existing open TCP connections will continue working, Only new DNS lookups fail, So impact may appear gradually.

5) Kubernetes Control Plane Usually Still Works ->
Nodes remain Ready, Pods remain Running, Kubectl still works, Cluster doesn't crash, Because core components don't depend heavily on CoreDNS for internal operations.

-----------------------------------------------------------------------------------------------------------------------------

Q. 561) What are the different types of services in Kubernetes?
Q. 562) There is an EC2 instance in a public subnet; how will you migrate it to a private subnet?
q. 563) What is Ingress in Kubernetes, and why do we need it?
Q. 564) Why should we use Kubernetes? What specific challenges does it solve?
Q. 565) Explain the detailed flow: How does a user request travel from a browser to a Kubernetes cluster?
Q. 566) What happens internally when you run kubectl apply -f deployment.yaml?
Q. 567) What is the difference between an API Gateway and Kubernetes Ingress?
Q. 568) How would you build a highly available and scalable architecture on AWS?
Q. 569) What is the difference between a NAT Instance and a NAT Gateway?
Q. 570) What is SonarQube? How do you integrate it with Jenkins, and what real-life issues have you faced during integration?
Q. 571) What are the different Routing Policies available in Route 53?
Q. 572) Explain the request flow from a Load Balancer to an EC2 web server.
Q. 573) How can you migrate a web server hosted on an EC2 instance to another AWS region?
-> 
---------------------------------------------------------------------------------------

Q. 574) How do you handle "Infrastructure Drift" when using Terraform?
-> Drift happens when the real infrastructure changes outside Terraform (manual change, script, console update, etc) so Terraform state and actual infra become different.
Infrastructure drift occurs when the actual infrastructure differes from Terraform state file, usually due to:
- Manual changes in cloud console (AWS /Azure / GCP)
- Changes by another automation tool
- Failed Terraform apply
- State file curruption
Terraform always compares:
Terraform state vs Real Infrastructure vs Terraform Code
- How to Detect Drift in Terraform
1) Run Terraform plan
- terraform plan
this command will compare current state with real infra, if drift exist Terraform will show changes.
2) use Terraform refresh
- terraform refresh
it will update state file from real infra
3) use terraform plan-refresh-only
- terraform plan -refresh-only
this only checks drift without changing infra
Infrasture drift happens when actual infrastructure differs from Terraform state i detect drift using "terraform plan" or "terraform plan -refresh-only" to fix drift, i either run run "terraform apply", import resources, or update Terraform code. To prevent drift, I use remote backend, state locking, CI/CD pipelines, and restrict manual changes.

-----------------------------------------------------------------------------------

Q. 575) awk '{gsub("ms","",$3); sum += $3; count++} END {if (count > 0) print "Average:", sum/count, "ms"; else print "No data"}' /tmp/1880973-linux-calculating-average-response-times-for-distributed-services/db.*.log

-----------------------------------------------------------------------------------------

Q. 576) what is the meaning of free -m and free -h command in linux?
-> 1) free -m -> free - m means display memory in mega bites format
   2) free -h -> free -h means display memory in human readable format, it's automatically displays memory in KB, MB, or GB, which is easier to read.

----------------------------------------------------------------------------------------------

Q. 577) when i am new in linx, how i comes to know file and directory in linux?
-> when we are new in linux we can use some command in linux to identify file or directory
-> using ls -l
output -> 
drwxr-xr-x 2 user user 4096 Mar  9 10:20 documents
-rw-r--r-- 1 user user 1200 Mar  9 10:15 file.txt

First character
d - Directory
- - Regular file
| 0- Symbolic link

----------------------------------------------------------

Q. 578) ping command work under which protocol?
-> The ping command works using the Internet Control Message Protocol (ICMP).

----------------------------------------------------------

Q. 579) how to build docker file using different name?
-> if you want to build a Docker image using a Docker file with different name, you can use -f option in the 
docker build command

-> docker build -f <dockerfile> -t <image-name>:<tag> .

----------------------------------------------------------------

Q. 580) what is the best practice to create a docker image using docker file to run or avoid as a root user
and channe the user with permission in docker file?
-> The best practice is to avoid running containers as root by creating a dedicated user inside the 
Dockerfile using userass and switching to it with the USER instruction. Also assign proper file ownership
using chown or COPY --chown to ensure the application runs with least privilege.

- Exampkle Dockerfile (Best Practice) ->

FROM ubuntu:22.04

# Create group and user
RUN groupadd -r appgroup && useradd -r -g appgroup appuser

# Create application directory
WORKDIR /app

# Copy application files
COPY . /app

# Change ownership of files
RUN chown -R appuser:appgroup /app

# Switch to non-root user
USER appuser

# Run application
CMD ["./start.sh"]

--------------------------------------------------------------------

Q. 581) what is the latest version of docker and kubernetes?
-> Docker version 29.0.0, kubernetes Latest stable version: 1.35.2 

-------------------------------------------------------------------

Q. 582) if we don't mention anything by default which port will be used for docker container?
-> Docker does not autoamatic chose a port, So the default port comes from the application, not Docker.
like Nginx → listens on port 80
Apache HTTP Server → listens on port 80
Tomcat → listens on port 8080

-------------------------------------------------------------------------------------

Q. 583) how to login inside the container?
-> docker exec -it <container_name_or_id> /bin/bash

-------------------------------------------------------------------------

Q. 584) what command will you used to destroy a resource in Terraform?
-> - All the resources -> terraform destroy
   - specific resource -> terraform destroy -target=<resource_type.resource_name>

---------------------------------------------------------------------------

Q. 585) difference between ingress controller and api gateway?
Q. 586) what is AWS direct connect?
Q. 587) what is the role of ingress controller?
Q. 588) Different modes in ansible?
-> 
---------------------------------------------------------------------------

Q. 589) Ansible lint.
-> Ansible Lint is a tool used to analyze Ansible playbooks for syntax errors, coding standards, and best practices before executing them.

--------------------------------------------------------------------------

Q. 590) modules in ansible.

--------------------------------------------------------------------------

Q. 591) how to see what containers are there under a k8s pod?
-> Use kubectl describe pod <pod-name> or kubectl get pod <pod-name> -o yaml to see the containers running inside a Kubernetes Pod.

--------------------------------------------------------------------------

Q. 592) variable precedence in terraform.
Q. 593) prometheus setup?
Q. 594) ELK path of the server where prometheus is installed?

-----------------------------------------------------------------

Q. 595) What is the docker layer?
-> A Docker layer is a read-only filesystem layer created for each instruction in a Dockerfile. Multiple layers stack together to form a Docker image, 
and containers add a writable layer on top of these image layers.

--------------------------------------------------------------------

Q. 596) what is the trust entity in IAM role?
-> A trusted entity in an IAM role is the AWS service, account, or identity that is allowed to assume the role through the role's trust policy.

-------------------------------------------------------------------------

Q. 597) what is the service account in kubernetes, how we can use?
-> A Service Account in Kubernetes provides an identity for Pods to interact with the Kubernetes API. 
It is used with RBAC roles and role bindings to control permissions for applications running inside the cluster.

---------------------------------------------------------------------------

Q. 598) in aws we have two account account1 have ec2 instance and account2 have s3 bucket, how we can connect both services?

-> If you have two AWS accounts:

Account 1 → has EC2 instance

Account 2 → has S3 bucket

You can allow the EC2 instance to access the S3 bucket using Cross-Account IAM Role or Bucket Policy. The best practice is IAM Role with cross-account access.

1️⃣ Method 1: Cross-Account IAM Role (Recommended)
Step 1: Create IAM Role in Account 2 (S3 account)

Go to IAM → Roles → Create Role

Choose Another AWS Account

Enter Account1 ID

Attach policy for S3 access.

Example policy:

{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": ["s3:GetObject","s3:ListBucket"],
      "Resource": [
        "arn:aws:s3:::my-bucket-name",
        "arn:aws:s3:::my-bucket-name/*"
      ]
    }
  ]
}

Trust relationship:

{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": {
        "AWS": "arn:aws:iam::ACCOUNT1-ID:root"
      },
      "Action": "sts:AssumeRole"
    }
  ]
}
Step 2: Attach IAM Role to EC2 in Account1

Create a role in Account1 with permission to assume the role in Account2.

Policy:

{
 "Version": "2012-10-17",
 "Statement": [
   {
     "Effect": "Allow",
     "Action": "sts:AssumeRole",
     "Resource": "arn:aws:iam::ACCOUNT2-ID:role/S3AccessRole"
   }
 ]
}

Attach this role to the EC2 instance.

Step 3: Assume Role from EC2

From the EC2 instance:

aws sts assume-role \
--role-arn arn:aws:iam::ACCOUNT2-ID:role/S3AccessRole \
--role-session-name testsession

Then use returned credentials to access S3.

2️⃣ Method 2: S3 Bucket Policy (Simpler)

You can directly allow Account1 EC2 IAM role in the S3 bucket policy.

Bucket policy in Account2:

{
 "Version": "2012-10-17",
 "Statement": [
   {
     "Effect": "Allow",
     "Principal": {
       "AWS": "arn:aws:iam::ACCOUNT1-ID:role/EC2Role"
     },
     "Action": "s3:*",
     "Resource": [
       "arn:aws:s3:::my-bucket-name",
       "arn:aws:s3:::my-bucket-name/*"
     ]
   }
 ]
}
3️⃣ Real DevOps Interview Answer (Short Version)

If this comes in interview, answer like:

“To connect EC2 in Account1 with an S3 bucket in Account2, we configure cross-account access using IAM Role or S3 bucket policy. The recommended approach is creating an IAM role in Account2 with S3 permissions and allowing Account1 to assume that role using STS AssumeRole.”

✅ Tip for DevOps interviews (6+ years)
Interviewers usually expect you to mention these keywords:

Cross Account Access

IAM Role

STS AssumeRole

Bucket Policy

-----------------------------------------------------------------------------------------------------------

Q. 599) How to change the terraform worksapce using terraform command?
-> terraform workspace select <workspace-name>

-------------------------------------------------------------------------------------------------

Q. 600) what is the null resource in terraform?
-> In Terraform, a null resource is a special resource that does not create any actual infrastructure. 
It is mainly used to run scripts, commands, or trigger actions inside Terraform.

-----------------------------------------------------------------------------------------------

Q. 601) what if our main.tf file is empty and we are going to terraform plan?
-> If your main.tf file is empty and you run terraform plan in Terraform, Terraform will not plan to create, modify, or destroy anything.
   If the state file already contains resources, but your main.tf is empty, then Terraform will plan to destroy those resources.

------------------------------------------------------------------------------------------------
